---
phase: 07-monetization
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - lib/features/settings/presentation/screens/subscription_screen.dart
  - lib/features/settings/presentation/widgets/upgrade_prompt.dart
  - lib/features/settings/presentation/widgets/feature_lock_overlay.dart
  - lib/core/subscription/subscription_cubit.dart
  - lib/features/couples/presentation/cubits/couples_feature_gate.dart
  - lib/features/family/presentation/cubits/family_feature_gate.dart
autonomous: true
requirements:
  - MON-03
  - MON-04
  - MON-05

must_haves:
  truths:
    - "Users can view their current subscription status and tier"
    - "Paywalled features show upgrade prompt when user lacks entitlement"
    - "Users can upgrade to Plus directly from the app"
    - "Subscription status updates in real-time after purchase"
  artifacts:
    - path: "lib/features/settings/presentation/screens/subscription_screen.dart"
      provides: "Main subscription management UI"
    - path: "lib/features/settings/presentation/widgets/upgrade_prompt.dart"
      provides: "Reusable upgrade prompt component"
    - path: "lib/core/subscription/subscription_cubit.dart"
      provides: "BLoC for subscription state management"
    - path: "lib/features/couples/presentation/cubits/couples_feature_gate.dart"
      provides: "Feature gating for couples features"
  key_links:
    - from: "subscription_screen.dart"
      to: "subscription_cubit"
      via: "BlocProvider"
      pattern: "BlocProvider.*SubscriptionCubit"
    - from: "upgrade_prompt.dart"
      to: "revenuecat_paywall"
      via: "Present paywall"
      pattern: "RevenueCat.*presentPaywall"
---

<objective>
Implement the subscription UI components and feature gating integration — enabling users to view their subscription, see upgrade prompts on locked features, and complete upgrade flow.

Purpose: Without this, users can't see their subscription status or know how to upgrade. This is the user-facing part of monetization.
Output: Working subscription UI with upgrade flow and feature gating integration
</objective>

<context>
@.planning/phases/07-monetization/07-CONTEXT.md
@.planning/phases/07-monetization/07-01-PLAN.md (from summary - subscription infrastructure exists)
@.planning/codebase/CONVENTIONS.md (Flutter UI patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Subscription Cubit</name>
  <files>lib/core/subscription/subscription_cubit.dart</files>
  <action>
Create BLoC/Cubit for subscription state:

1. States:
   - SubscriptionInitial
   - SubscriptionLoading
   - SubscriptionLoaded(tier, status, expiresAt, isInGracePeriod)
   - SubscriptionError(message)

2. Events/Methods:
   - loadSubscriptionStatus() - fetches from local cache then verifies with API
   - refreshStatus() - forces API call
   - checkFeatureEntitlement(String featureId) - returns bool

3. Use SubscriptionService from plan 01
4. Add StreamSubscription to listen for subscription changes
5. Emit state changes based on entitlement check results
</action>
  <verify>
<automated>dart analyze lib/core/subscription/ 2>&1 | grep -i "error\|warning"</automated>
  </verify>
  <done>SubscriptionCubit loads and manages subscription state correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create Subscription Status Screen</name>
  <files>lib/features/settings/presentation/screens/subscription_screen.dart</files>
  <action>
Create subscription management screen in Settings:

1. Header: Show current tier with icon
   - Free: "Free Plan" with muted styling
   - Plus: "Tether Plus" with accent glow
   - Family: "Family Plan" with family styling

2. Current benefits section:
   - List what's included in current tier
   - Mark paid features as locked/unlocked with icons

3. Upgrade section (if free):
   - "Unlock Tether Plus" button
   - Show monthly/annual pricing
   - "Best value" badge on annual
   - Tap opens RevenueCat paywall

4. Subscription details (if plus):
   - Current plan name
   - Renewal date / Expiration date
   - "Manage Subscription" button (links to App Store/Play Store)

5. Grace period banner (if applicable):
   - "Your Plus access continues until [date]"
   - Soft warning color

Use current time-theme tokens for styling.
</action>
  <verify>
<automated>flutter analyze lib/features/settings/ 2>&1 | grep -i "error\|warning" | head -20</automated>
  </verify>
  <done>Screen displays correct subscription status and upgrade options</done>
</task>

<task type="auto">
  <name>Task 3: Create Reusable Upgrade Prompt</name>
  <files>lib/features/settings/presentation/widgets/upgrade_prompt.dart</files>
  <action>
Create reusable upgrade prompt widget:

1. Modal/Bottom sheet design
2. Content:
   - Lock icon with glow effect
   - "Unlock [Feature Name]" title
   - Brief description of what's gained
   - "Upgrade to Tether Plus" primary button
   - "Maybe later" secondary button

3. Button handler - RevenueCat SDK integration:
   ```dart
   // On "Upgrade" button tap:
   final purchases = Purchases.getSharedInstance();
   await purchases.presentPaywall();
   // OR with specific offering:
   await purchases.presentPaywall(offering: offering);
   ```

4. Triggered when user taps paywalled feature without Plus
5. Different messaging per feature:
   - Voice Notes: "Send voice messages to your circles"
   - Couples: "Unlock private moments with your partner"
   - Family: "Safety features for your family"
   - Letter Mode: "Send thoughtful letters on your schedule"
   - Future Letters: "Send time capsule messages to the future"
6. Animate in with scale + fade (300ms ease-out)

Use TETHER_PRD.md section 11 for feature-to-tier mapping.
</action>
<verify>
<automated>flutter analyze lib/features/settings/ 2>&1 | grep -i "error" | head -10</automated>
</verify>
<done>Upgrade prompt appears with correct feature-specific messaging and RevenueCat paywall opens</done>
</task>

<task type="auto">
  <name>Task 4: Integrate feature gating in couples and family features</name>
  <files>lib/features/couples/presentation/cubits/couples_feature_gate.dart
        lib/features/family/presentation/cubits/family_feature_gate.dart</files>
  <action>
Create feature gating cubits:

1. CouplesFeatureGate:
   - Check entitlement for couples features (Our Bubble, Heartbeat, etc.)
   - Methods: canAccessOurBubble(), canAccessHeartbeat(), canAccessGoodMorningNight()
   - If denied, return false and trigger upgrade prompt

2. FamilyFeatureGate:
   - Check entitlement for family features (Safety Check, SOS, etc.)
   - Methods: canAccessSafetyCheck(), canAccessSOS(), canAccessHeritageCorner()
   - If denied, return false and trigger upgrade prompt

3. Integrate into existing feature cubits:
   - In CouplesCubit - add entitlement check before enabling features
   - In FamilyCubit - add entitlement check before enabling features
   - Show upgrade prompt overlay when entitlement check fails

Use SubscriptionService.checkEntitlement() from plan 01.
</action>
  <verify>
<automated>flutter analyze lib/features/couples/ lib/features/family/ 2>&1 | grep -i "error" | head -10</automated>
  </verify>
  <done>Couples and family features properly gated behind subscription</done>
</task>

<task type="auto">
  <name>Task 5: Create PPP pricing display utility</name>
  <files>lib/core/subscription/pricing_display.dart</files>
  <action>
Create utility for PPP-adjusted pricing:

1. Primary: Fetch prices from RevenueCat offering configuration
   - Use RevenueCat SDK: Purchases.getOfferings() to get live pricing
   - Filter offerings by user region/country from device locale

2. Fallback (offline/caching): Map country to PPP-adjusted price tier:
   - Tier 1 (USD base): US, UK, Canada, Australia
   - Tier 2 (70% of USD): Germany, France, Japan, South Korea
   - Tier 3 (50% of USD): India, Brazil, Mexico, Indonesia
   - Tier 4 (35% of USD): Pakistan, Nigeria, Bangladesh

3. Format price with correct currency symbol
4. Show regional price in upgrade prompts (not USD unless in Tier 1)
5. Cache prices locally with TTL for offline access

Reference RevenueCat's offering configuration per region.
</action>
<verify>
<automated>dart analyze lib/core/subscription/ 2>&1 | grep -i "error"</automated>
</verify>
<done>Pricing fetches from RevenueCat or falls back to PPP tiers</done>
</task>

</tasks>

<verification>
1. Run flutter analyze on new files
2. Verify all imports resolve
3. Test upgrade prompt appears on locked feature tap
4. Verify subscription status screen shows correct tier
</verification>

<success_criteria>
- Subscription screen shows current tier with correct styling
- Upgrade prompts appear when accessing locked features
- Feature gating blocks paid features for free users
- PPP pricing displays correctly per region
- Grace period banner shows when applicable
</success_criteria>

<output>
After completion, create `.planning/phases/07-monetization/07-02-SUMMARY.md`</output>