---
phase: 12
wave: 3
depends_on: ["12-03"]
files_modified: ["lib/features/monetization/presentation/screens/checkout_screen.dart", ".github/workflows/build-release.yml"]
autonomous: true
---

# Plan 12-04: Payments UI & Launch Pipelines

## Objective
Provide the user choice for the payment checkout layer and implement the CI/CD instructions mapping App Store releases to Fastlane equivalents.

## Tasks

### 1. Build Checkout Screen
<read_first>
- lib/features/monetization/presentation/screens/checkout_screen.dart (New)
- lib/core/theme/app_theme.dart
</read_first>
<action>
Create a beautifully styled `CheckoutScreen` (StatelessWidget for now) utilizing cards with `AppTheme` styles. It must feature two large buttons:
"Pay via App Store" and "Alternative Checkout (Razorpay)".
When the user taps either, it triggers a cubit event sending them to `IBillingRepository`.
</action>
<acceptance_criteria>
- `CheckoutScreen` contains button labels explicitly defining Razorpay versus Native.
</acceptance_criteria>

### 2. Add CI/CD App Store Automation Workflows
<read_first>
- .github/workflows/build-release.yml (New)
</read_first>
<action>
Create a GitHub Actions workflow using `on: release` triggering flutter tests natively and deploying iOS Fastlane to TestFlight, Android to Play Store Internal Track. Provide necessary placeholders like `APPLE_ISSUER_ID`.
</action>
<acceptance_criteria>
- `.github/workflows/build-release.yml` exists.
- The YAML contains `fastlane android` and `fastlane ios` steps.
</acceptance_criteria>

## Verification
- GitHub action YAML is syntactically well-formed, readable by YAML validators.
