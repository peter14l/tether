---
phase: 12
wave: 2
depends_on: ["12-02"]
files_modified: ["pubspec.yaml", "lib/features/monetization/domain/repositories/billing_repository.dart", "lib/features/monetization/data/repositories/billing_repository_impl.dart"]
autonomous: true
---

# Plan 12-03: Monetization Logic (IAP vs Razorpay)

## Objective
Implement flexible billing abstractions that decouple the UI from the underlying payment processor choice.

## Tasks

### 1. Add packages and scaffold domain
<read_first>
- pubspec.yaml
</read_first>
<action>
Add `in_app_purchase: ^3.1.11` and `razorpay_flutter: ^1.3.6` to `pubspec.yaml`.
Create `lib/features/monetization/domain/repositories/billing_repository.dart` with an interface `IBillingRepository` that has a method `Future<Either<Failure, void>> purchasePremium({required bool useRazorpay});`.
</action>
<acceptance_criteria>
- `in_app_purchase` is in pubspec configurations.
- `IBillingRepository` exists.
</acceptance_criteria>

### 2. Implement the Unified Billing Repository
<read_first>
- lib/features/monetization/data/repositories/billing_repository_impl.dart (New)
</read_first>
<action>
Implement `BillingRepositoryImpl` that branches logic. 
If `useRazorpay == true`, invoke dummy Razorpay init logic (calling `Razorpay().open(options)`).
If `useRazorpay == false`, use `InAppPurchase.instance.buyNonConsumable(purchaseParam)`.
Catch all platform errors into `Failure`. Register `BillingRepositoryImpl` as a singleton in `injection_container.dart`.
</action>
<acceptance_criteria>
- File binds both `Razorpay()` and `InAppPurchase.instance`.
</acceptance_criteria>

## Verification
- Run `flutter analyze` without compilation errors.
