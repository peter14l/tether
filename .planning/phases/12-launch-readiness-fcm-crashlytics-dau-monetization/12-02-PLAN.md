---
phase: 12
wave: 1
depends_on: []
files_modified: ["supabase/migrations/0008_fcm_tokens_and_subscriptions.sql", "lib/features/messaging/data/repositories/supabase_messaging_repository.dart"]
autonomous: true
---

# Plan 12-02: Notifications Infrastructure (FCM + Supabase)

## Objective
Create the push notification schema dependencies mapping Firebase tokens to Supabase User UI.

## Tasks

### 1. Database Schema Extensions
<read_first>
- supabase/migrations/0008_fcm_tokens_and_subscriptions.sql (New)
</read_first>
<action>
Create a new migration `supabase/migrations/0008_fcm_tokens_and_subscriptions.sql` that:
1. Creates table `user_devices (user_id UUID, fcm_token TEXT, created_at TIMESTAMPTZ, PRIMARY KEY (user_id, fcm_token))`.
2. Alters the `users` (or auth/public equivalent if using profiles) or just creates `public.profiles` column `subscription_tier TEXT DEFAULT 'free'`. We will just use `user_devices` and a new `users_subscription` table for separation if `profiles` is too broad. Actually, create `user_subscriptions (user_id UUID PRIMARY KEY, tier TEXT DEFAULT 'free', valid_until TIMESTAMPTZ)`.
3. Apply standard RLS to `user_devices` and `user_subscriptions` (Users can only see/insert their own IDs).
</action>
<acceptance_criteria>
- Migration SQL file exists and contains `CREATE TABLE user_devices` and `CREATE TABLE user_subscriptions`.
</acceptance_criteria>

### 2. [BLOCKING] Apply Schema Migrations
<read_first>
- supabase/migrations/0008_fcm_tokens_and_subscriptions.sql
</read_first>
<action>
Run `supabase db push` to push the new tables and RLS rules to the database. Flag for manual override if prompted.
</action>
<acceptance_criteria>
- Database actively features the new tables.
</acceptance_criteria>

### 3. Add PushNotificationService Wrapper
<read_first>
- pubspec.yaml
- lib/core/notifications/push_notification_service.dart (New)
</read_first>
<action>
Add `firebase_messaging` to pubspec.yaml.
Create `PushNotificationService` that retrieves `FirebaseMessaging.instance.getToken()` and upserts it into the `user_devices` Supabase table using `SupabaseClient.instance`. 
</action>
<acceptance_criteria>
- `push_notification_service.dart` exposes a mechanism to store FCM token via Supabase.
</acceptance_criteria>

## Verification
- Verify running `supabase db push` does not fail and tables exist in Supabase local studio.
