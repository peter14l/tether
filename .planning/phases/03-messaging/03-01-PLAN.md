---
phase: 03-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/features/messaging/domain/entities/message_entity.dart, lib/features/messaging/domain/entities/voice_note_entity.dart, lib/features/messaging/data/models/message_model.dart, lib/features/messaging/data/models/voice_note_model.dart, lib/features/messaging/domain/repositories/messaging_repository.dart, lib/features/messaging/data/repositories/supabase_messaging_repository.dart, lib/features/wellness/domain/repositories/presence_repository.dart, lib/features/wellness/data/repositories/supabase_presence_repository.dart, lib/injection_container.config.dart]
autonomous: true
requirements: [MSG-01, MSG-02, PRES-01, PRES-02]
must_haves:
  truths:
    - "Messaging entities and models are defined matching the Supabase schema"
    - "Messaging repository provides methods for fetching and sending messages"
    - "Presence repository provides methods for toggling Quiet mode and tracking real-time status"
  artifacts:
    - path: "lib/features/messaging/domain/entities/message_entity.dart"
      provides: "Message domain representation"
    - path: "lib/features/messaging/data/repositories/supabase_messaging_repository.dart"
      provides: "Supabase implementation of messaging repo"
    - path: "lib/features/wellness/data/repositories/supabase_presence_repository.dart"
      provides: "Supabase implementation of presence repo"
  key_links:
    - from: "lib/features/messaging/data/repositories/supabase_messaging_repository.dart"
      to: "supabase.from('messages')"
      via: "Postgrest/Realtime"
---

<objective>
Define the core Domain and Data layers for Messaging and Presence features.
This includes entities, models, and Supabase repository implementations for DMs, Voice Notes, and Quiet Presence status.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Messaging Domain & Data</name>
  <files>lib/features/messaging/domain/entities/message_entity.dart, lib/features/messaging/domain/entities/voice_note_entity.dart, lib/features/messaging/data/models/message_model.dart, lib/features/messaging/data/models/voice_note_model.dart, lib/features/messaging/domain/repositories/messaging_repository.dart, lib/features/messaging/data/repositories/supabase_messaging_repository.dart</files>
  <action>
    - Define MessageEntity and VoiceNoteEntity in domain.
    - Implement MessageModel and VoiceNoteModel with fromJson/toJson for Supabase.
    - Define IMessagingRepository interface with methods:
      - getMessages(String circleId or userIds)
      - sendMessage(Message message)
      - streamMessages(String circleId or userIds)
      - uploadVoiceNote(File file)
    - Implement SupabaseMessagingRepository using SupabaseClient.
    - Register in GetIt using injectable.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Messaging core domain and data layers implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 2: Presence Domain & Data</name>
  <files>lib/features/wellness/domain/repositories/presence_repository.dart, lib/features/wellness/data/repositories/supabase_presence_repository.dart</files>
  <action>
    - Define IPresenceRepository interface.
    - Implement SupabasePresenceRepository to:
      - Toggle Quiet mode (update 'profiles' table).
      - Track circle members' presence using Supabase Realtime Presence channels.
      - Stream presence updates.
    - Register in GetIt.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>Presence core domain and data layers implemented and registered.</done>
</task>

<task type="auto">
  <name>Task 3: Dependencies & Storage Setup</name>
  <files>pubspec.yaml</files>
  <action>
    - Add dependencies: `record`, `audioplayers`, `path_provider`, `permission_handler`.
    - Note: Supabase Storage buckets 'voice-notes' and 'circle-media' are assumed to exist per PRD 6.3.
    - Run flutter pub get.
  </action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>Audio dependencies added and installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Supabase | Untrusted input (messages, voice notes) sent to backend |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Message Content | mitigate | Use client-side encryption for sensitive content as per PRD 10 |
| T-03-01-02 | Information Disclosure | Presence Status | mitigate | RLS ensures only circle members see presence status |
</threat_model>

<success_criteria>
- Messaging and Presence repositories implemented and registered in DI.
- Models match Supabase schema.
- Audio dependencies installed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-messaging/03-01-SUMMARY.md`
</output>
