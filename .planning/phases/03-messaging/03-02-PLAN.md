---
phase: 03-messaging
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [lib/features/messaging/presentation/bloc/messaging_cubit.dart, lib/features/messaging/presentation/bloc/messaging_state.dart, lib/features/wellness/presentation/bloc/presence_cubit.dart, lib/features/wellness/presentation/bloc/presence_state.dart, lib/injection_container.config.dart]
autonomous: true
requirements: [MSG-01, PRES-01, PRES-02]
must_haves:
  truths:
    - "MessagingCubit handles real-time DM streams and sending messages"
    - "PresenceCubit handles toggling Quiet mode and tracking circle member presence"
  artifacts:
    - path: "lib/features/messaging/presentation/bloc/messaging_cubit.dart"
      provides: "DMs state management"
    - path: "lib/features/wellness/presentation/bloc/presence_cubit.dart"
      provides: "Presence state management"
  key_links:
    - from: "lib/features/messaging/presentation/bloc/messaging_cubit.dart"
      to: "lib/features/messaging/domain/repositories/messaging_repository.dart"
      via: "dependency injection"
---

<objective>
Implement BLoCs (Cubits) for Messaging and Presence features to handle business logic and state.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@.planning/phases/03-messaging/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Messaging BLoC</name>
  <files>lib/features/messaging/presentation/bloc/messaging_cubit.dart, lib/features/messaging/presentation/bloc/messaging_state.dart</files>
  <action>
    - Implement MessagingCubit with methods:
      - loadMessages(String circleId or userIds)
      - sendMessage(String text, {String? type, String? mediaUrl})
      - subscribeToMessages(String circleId or userIds)
    - Handle loading, success, and error states for message history and real-time updates.
    - Handle read receipts (if user enables them).
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>MessagingCubit implemented for managing DM state.</done>
</task>

<task type="auto">
  <name>Task 2: Presence BLoC</name>
  <files>lib/features/wellness/presentation/bloc/presence_cubit.dart, lib/features/wellness/presentation/bloc/presence_state.dart</files>
  <action>
    - Implement PresenceCubit with methods:
      - toggleQuietMode(bool enabled, {DateTime? until})
      - trackCirclePresence(String circleId)
    - Monitor real-time presence of circle members.
    - Map 'profiles' quiet status to presence state.
  </action>
  <verify>
    <automated>flutter pub run build_runner build --delete-conflicting-outputs</automated>
  </verify>
  <done>PresenceCubit implemented for managing presence state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Presentation → Domain | Input from UI passed to repositories |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | Messaging Stream | mitigate | Ensure streams are properly closed on dispose to prevent resource leaks |
</threat_model>

<success_criteria>
- MessagingCubit and PresenceCubit implemented and registered in DI.
- Cubits correctly handle state for messaging and presence updates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-messaging/03-02-SUMMARY.md`
</output>
