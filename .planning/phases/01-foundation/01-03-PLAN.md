---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [lib/injection_container.dart, lib/core/router/app_router.dart, lib/main.dart]
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "Dependency Injection (GetIt/Injectable) is set up and working"
    - "GoRouter manages the application's basic navigation"
  artifacts:
    - path: "lib/injection_container.dart"
      provides: "DI configuration"
    - path: "lib/core/router/app_router.dart"
      provides: "Navigation configuration"
  key_links:
    - from: "lib/main.dart"
      to: "lib/injection_container.dart"
      via: "DI initialization"
    - from: "lib/main.dart"
      to: "lib/core/router/app_router.dart"
      via: "Router configuration"
---

<objective>
Configure Dependency Injection and basic routing for the application.

Purpose: Enable decoupled architecture and consistent navigation.
Output: DI initialization container and centralized GoRouter configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependency Injection with GetIt and Injectable</name>
  <files>lib/injection_container.dart</files>
  <action>Create lib/injection_container.dart and configure Injectable.
  Run `flutter pub run build_runner build --delete-conflicting-outputs` to generate the injection glue.
  Register `SupabaseClient` and other core utilities as lazy singletons.</action>
  <verify>
    <automated>flutter pub run build_runner build</automated>
  </verify>
  <done>Injectable generates the necessary code and `initGetIt` is available for usage in `main.dart`.</done>
</task>

<task type="auto">
  <name>Task 2: Setup basic GoRouter configuration</name>
  <files>lib/core/router/app_router.dart</files>
  <action>Create lib/core/router/app_router.dart and define the initial routes:
  - '/' (Root/Home placeholder)
  - '/login' (Login placeholder)
  Integrate the router into `MaterialApp.router` in `lib/main.dart`.</action>
  <verify>
    <automated>dart analyze lib/core/router/app_router.dart</automated>
  </verify>
  <done>The app uses GoRouter for navigation and defines basic routing paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal → DI Container | Component wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Elevation of Privilege | App Routing | mitigate | Implement router guards in future phases to protect private routes. |
</threat_model>

<verification>
Verify `flutter pub run build_runner build` succeeds and `main.dart` uses `MaterialApp.router`.
</verification>

<success_criteria>
- Injection container generates `injection_container.config.dart` successfully.
- Basic routes for Home and Login are defined.
- Main app is wrapped in `MaterialApp.router`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
