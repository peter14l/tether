---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, lib/core/error/failures.dart, lib/core/usecase/usecase.dart, lib/core/network/network_info.dart]
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "Flutter project has the Clean Architecture folder structure defined in PRD 5.2"
    - "Required dependencies for Supabase, BLoC, DI, and Encryption are present in pubspec.yaml"
    - "Core base classes for Failures and UseCases exist"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Project dependencies"
    - path: "lib/core/error/failures.dart"
      provides: "Standard error types"
    - path: "lib/core/usecase/usecase.dart"
      provides: "Base UseCase interface"
  key_links:
    - from: "lib/main.dart"
      to: "lib/core"
      via: "imports"
---

<objective>
Initialize the Flutter project structure for Clean Architecture and add necessary dependencies.

Purpose: Provide the foundational scaffolding for all future features.
Output: Standardized folder structure, updated dependencies, and base domain classes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Clean Architecture folder structure</name>
  <files>lib/core, lib/features, lib/core/theme, lib/core/error, lib/core/usecase, lib/core/network, lib/core/utils</files>
  <action>Create the directory structure as defined in PRD 5.2. Ensure the following exist:
  - lib/core/theme
  - lib/core/error
  - lib/core/usecase
  - lib/core/network
  - lib/core/utils
  - lib/features/auth (as the first feature placeholder)</action>
  <verify>
    <automated>ls -R lib/core lib/features</automated>
  </verify>
  <done>Folder structure exists exactly as specified in the PRD.</done>
</task>

<task type="auto">
  <name>Task 2: Update pubspec.yaml with required dependencies</name>
  <files>pubspec.yaml</files>
  <action>Add the following dependencies to pubspec.yaml (check for latest versions):
  - supabase_flutter: ^2.5.0
  - flutter_bloc: ^8.1.6
  - get_it: ^7.7.0
  - injectable: ^2.5.0
  - go_router: ^14.2.0
  - cryptography: ^2.5.0
  - flutter_secure_storage: ^9.2.2
  - equatable: ^2.0.5
  - fpdart: ^0.6.0
  - lora: ^1.0.1 (Check if these fonts need explicit pub packages or asset declarations)
  - google_fonts: ^6.2.1 (Easier for PRD fonts Playfair Display, Lora, DM Sans)
  
  Dev dependencies:
  - build_runner: ^2.4.9
  - injectable_generator: ^2.6.2
  
  Run `flutter pub get` after updating.</action>
  <verify>
    <automated>flutter pub get</automated>
  </verify>
  <done>All required packages are available in the project.</done>
</task>

<task type="auto">
  <name>Task 3: Define base classes for Domain layer</name>
  <files>lib/core/error/failures.dart, lib/core/usecase/usecase.dart</files>
  <action>Implement standard base classes:
  - lib/core/error/failures.dart: Abstract Failure class and common subclasses (ServerFailure, CacheFailure, AuthFailure).
  - lib/core/usecase/usecase.dart: UseCase interface with `call` method using fpdart's Either.</action>
  <verify>
    <automated>dart analyze lib/core/error/failures.dart lib/core/usecase/usecase.dart</automated>
  </verify>
  <done>Foundational classes for Clean Architecture are defined and free of syntax errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Untrusted network communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | pubspec.yaml | mitigate | Use version pinning and checksums (handled by pubspec.lock) |
</threat_model>

<verification>
Ensure the project structure matches PRD 5.2 exactly and `flutter pub get` passes.
</verification>

<success_criteria>
- Project folder structure matches PRD 5.2.
- `pubspec.yaml` contains all specified dependencies.
- `failures.dart` and `usecase.dart` exist with correct implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
