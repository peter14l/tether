---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified: [lib/core/utils/encryption_service.dart, lib/core/utils/key_derivation.dart, lib/core/utils/escrow_service.dart]
autonomous: true
requirements: [FOUND-04]

must_haves:
  truths:
    - "Encryption service provides AES-256-GCM encryption and decryption"
    - "Key derivation logic uses secure standards (e.g. PBKDF2 with salt)"
    - "Foundation for Cloud Escrow exists to store/retrieve encrypted master keys with PIN"
  artifacts:
    - path: "lib/core/utils/encryption_service.dart"
      provides: "AES-256-GCM core"
    - path: "lib/core/utils/key_derivation.dart"
      provides: "Key derivation utilities"
    - path: "lib/core/utils/escrow_service.dart"
      provides: "Key escrow foundation"
---

<objective>
Implement the core end-to-end encryption service and key management foundation.

Purpose: Protect sensitive user data (journals, vault) from unauthorized server access.
Output: Core cryptographic utilities and a secure key escrow system foundation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AES-256-GCM Encryption Service</name>
  <files>lib/core/utils/encryption_service.dart, lib/core/utils/key_derivation.dart</files>
  <action>Create lib/core/utils/encryption_service.dart using the `cryptography` package.
  Implement:
  - AES-256-GCM encryption/decryption.
  - PBKDF2 key derivation from a password/PIN and salt in `key_derivation.dart`.
  - Secure random salt generation.</action>
  <verify>
    <automated>dart analyze lib/core/utils/encryption_service.dart</automated>
  </verify>
  <done>Encryption and key derivation are implemented correctly and free of syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cloud Escrow foundation logic</name>
  <files>lib/core/utils/escrow_service.dart</files>
  <action>Create lib/core/utils/escrow_service.dart with logic to:
  - Generate a random Master Key for the user on first setup.
  - Encrypt the Master Key with a key derived from a user-provided PIN.
  - Prepare the payload (encrypted master key + salt + IV) for escrow in Supabase (the actual API call for storage can be a mock/placeholder until Auth is fully wired in the next phase).</action>
  <verify>
    <automated>dart analyze lib/core/utils/escrow_service.dart</automated>
  </verify>
  <done>The logic for secure PIN-based key recovery foundation is defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Memory → Persistent Storage | Sensitive keys handled in memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Memory/Storage | mitigate | Use `flutter_secure_storage` for local persistence and ensure memory is cleared where possible. |
| T-01-04-02 | Spoofing | Encryption Keys | mitigate | Include auth tags (GCM) in encryption to detect tampering. |
</threat_model>

<verification>
Ensure `dart analyze` passes and verify the crypto logic follows best practices.
</verification>

<success_criteria>
- `EncryptionService` provides functional encryption/decryption methods.
- `KeyDerivation` uses PBKDF2 or equivalent secure derivation.
- `EscrowService` implements the master-key-encrypted-by-PIN logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
