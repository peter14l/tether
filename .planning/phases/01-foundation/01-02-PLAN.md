---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/0001_initial_schema.sql, lib/main.dart]
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "SQL migration file exists with definitions for all tables from PRD 6.2"
    - "Supabase client initialization logic is in main.dart"
  artifacts:
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "Initial database schema"
    - path: "lib/main.dart"
      provides: "App initialization entry point"
  key_links:
    - from: "lib/main.dart"
      to: "supabase_flutter"
      via: "package usage"
---

<objective>
Define the Supabase database schema and initialize the client in the app.

Purpose: Provide the persistent storage backend for the app.
Output: Comprehensive SQL migration file and functional Supabase client initialization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration file with all PRD tables</name>
  <files>supabase/migrations/0001_initial_schema.sql</files>
  <action>Create a single SQL migration file containing:
  - Table definitions from PRD 6.2: profiles, circles, circle_members, posts, reactions, mood_rooms, check_ins, etc.
  - Core RLS Policies from PRD 6.6.
  - Basic triggers for updated_at timestamps where applicable.
  Use Postgres gen_random_uuid() for primary keys where specified.</action>
  <verify>
    <automated>test -f supabase/migrations/0001_initial_schema.sql</automated>
  </verify>
  <done>SQL migration file exists with correct table definitions as specified in PRD 6.2 and 6.6.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Supabase client in main.dart</name>
  <files>lib/main.dart</files>
  <action>Update lib/main.dart to initialize Supabase with placeholder URL and Anon Key.
  Ensure `WidgetsFlutterBinding.ensureInitialized()` is called.
  Implement a safe way to store these keys (using environment variables or a separate config file if appropriate, but for now placeholders are fine for initialization logic).</action>
  <verify>
    <automated>dart analyze lib/main.dart</automated>
  </verify>
  <done>Supabase is correctly initialized within the Flutter app initialization flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Untrusted network communication |
| Supabase → Database | Database access controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | lib/main.dart | mitigate | Do not commit real Supabase keys; use environment variables in CI/CD. |
| T-01-02-02 | Elevation of Privilege | Supabase Tables | mitigate | Implement RLS policies exactly as specified in PRD 6.6. |
</threat_model>

<verification>
Check that `supabase/migrations/0001_initial_schema.sql` contains all tables listed in PRD 6.2.
</verification>

<success_criteria>
- Migration file contains schema for all 20+ tables from PRD.
- RLS policies are defined in the migration.
- `main.dart` compiles with Supabase initialization.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
