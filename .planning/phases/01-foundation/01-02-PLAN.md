---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [supabase/migrations/0001_initial_schema.sql, lib/main.dart]
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "SQL migration file exists with definitions for all tables from PRD 6.2"
    - "SQL migration includes messages and escrow_keys tables"
    - "Supabase client initialization logic is in main.dart"
  artifacts:
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "Initial database schema"
    - path: "lib/main.dart"
      provides: "App initialization entry point"
  key_links:
    - from: "lib/main.dart"
      to: "supabase_flutter"
      via: "package usage"
---

<objective>
Define the Supabase database schema and initialize the client in the app.

Purpose: Provide the persistent storage backend for the app.
Output: Comprehensive SQL migration file and functional Supabase client initialization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration file with all PRD tables + messaging and escrow</name>
  <files>supabase/migrations/0001_initial_schema.sql</files>
  <action>Create a single SQL migration file containing:
  - Table definitions from PRD 6.2: profiles, circles, circle_members, posts, reactions, mood_rooms, check_ins, etc.
  - NEW: `messages` table for DMs (sender_id, receiver_id, content_type, content_text, media_url, etc).
  - NEW: `escrow_keys` table for Cloud Escrow strategy (user_id, encrypted_master_key, salt, iv).
  - Core RLS Policies from PRD 6.6.
  - Basic triggers for updated_at timestamps.
  Use Postgres gen_random_uuid() for primary keys where specified.</action>
  <verify>
    <automated>grep -E "CREATE TABLE (messages|escrow_keys)" supabase/migrations/0001_initial_schema.sql</automated>
  </verify>
  <done>SQL migration file exists with correct table definitions as specified in PRD 6.2 and 6.6, including messages and escrow tables.</done>
</task>

<task type="auto">
  <name>Task 2: Initial Supabase client setup in main.dart</name>
  <files>lib/main.dart</files>
  <action>Update lib/main.dart to initialize Supabase.
  - Call `WidgetsFlutterBinding.ensureInitialized()`.
  - Use `Supabase.initialize` with placeholder URL and Anon Key.
  - Wrap the root Widget with Supabase client provider if necessary, but at minimum ensure the singleton is initialized.
  
  Note: Plan 03 will handle the final wiring with Dependency Injection.</action>
  <verify>
    <automated>dart analyze lib/main.dart</automated>
  </verify>
  <done>Supabase client is initialized within the Flutter app initialization flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Untrusted network communication |
| Supabase → Database | Database access controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | lib/main.dart | mitigate | Use placeholders for keys; real keys should be in env vars/secrets. |
| T-01-02-02 | Elevation of Privilege | Supabase Tables | mitigate | Implement RLS policies exactly as specified in PRD 6.6. |
</threat_model>

<verification>
Check that `supabase/migrations/0001_initial_schema.sql` contains all tables from PRD and the new messaging/escrow tables.
</verification>

<success_criteria>
- Migration file contains schema for all 20+ tables from PRD.
- RLS policies are defined in the migration.
- `main.dart` compiles with Supabase initialization.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
