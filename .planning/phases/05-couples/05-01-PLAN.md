---
phase: 05-couples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/0002_couples.sql, lib/features/couples/domain/entities/couple_bubble.dart, lib/features/couples/domain/entities/digital_hug.dart, lib/features/couples/domain/entities/heartbeat.dart, lib/features/couples/domain/entities/milestone.dart, lib/features/couples/domain/entities/gallery_item.dart, lib/features/couples/domain/repositories/couples_repository.dart, lib/features/couples/domain/repositories/milestones_repository.dart, lib/features/couples/domain/repositories/gallery_repository.dart, lib/features/couples/data/models/couple_bubble_model.dart, lib/features/couples/data/models/digital_hug_model.dart, lib/features/couples/data/models/heartbeat_model.dart, lib/features/couples/data/models/milestone_model.dart, lib/features/couples/data/models/gallery_item_model.dart]
autonomous: true
requirements: [COUP-11, COUP-12, COUP-14, COUP-15]

must_haves:
  truths:
    - "Supabase schema includes necessary columns for 'Space to Breathe' in couple_bubble"
    - "Tables for 'relationship_milestones' and 'private_gallery' exist in Supabase"
    - "Domain entities for all couples features are defined correctly"
  artifacts:
    - path: "supabase/migrations/0002_couples.sql"
      provides: "Updated schema for couples features"
    - path: "lib/features/couples/domain/entities/couple_bubble.dart"
      provides: "Couple Bubble core entity"
    - path: "lib/features/couples/domain/repositories/couples_repository.dart"
      provides: "Couples interaction contract"
  key_links:
    - from: "lib/features/couples/data/models/couple_bubble_model.dart"
      to: "lib/features/couples/domain/entities/couple_bubble.dart"
      via: "inheritance/conversion"
---

<objective>
Initialize the Couples feature foundation including database schema updates, domain entities, and repository interfaces.

Purpose: Provide the structural and data-contract backbone for all couples-specific features.
Output: Supabase migration for couples tables, Clean Architecture entities, and repository interfaces.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/features/couples/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Schema Migration</name>
  <files>supabase/migrations/0002_couples.sql</files>
  <action>Create a new SQL migration to add missing couples-specific fields and tables:
  - Add `space_to_breathe_active` (boolean), `space_to_breathe_until` (timestamptz), and `space_to_breathe_by` (UUID fk to profiles) to `couple_bubble` table.
  - Create `relationship_milestones` table: id, circle_id (fk), event_date (date), title (text), description (text), category (text, 'auto'|'manual'), created_at.
  - Create `private_gallery` table: id, circle_id (fk), storage_path (text), caption (text), created_at, uploaded_by (fk).
  - Add RLS policies for `relationship_milestones` and `private_gallery` (only circle members can read/write).
  - Create `private-gallery` bucket in Supabase Storage with circle-member-only access policies.</action>
  <verify>
    <automated>Select-String -Pattern "CREATE TABLE" -Path supabase/migrations/0002_couples.sql</automated>
  </verify>
  <done>Supabase schema is updated for couples features with proper RLS and storage buckets.</done>
</task>

<task type="auto">
  <name>Task 2: Couples Domain Layer</name>
  <files>lib/features/couples/domain/entities/couple_bubble.dart, lib/features/couples/domain/entities/digital_hug.dart, lib/features/couples/domain/entities/heartbeat.dart, lib/features/couples/domain/entities/milestone.dart, lib/features/couples/domain/entities/gallery_item.dart, lib/features/couples/domain/repositories/couples_repository.dart, lib/features/couples/domain/repositories/milestones_repository.dart, lib/features/couples/domain/repositories/gallery_repository.dart</files>
  <action>Define the domain contracts for the Couples feature:
  - Create entities for `CoupleBubble`, `DigitalHug`, `Heartbeat`, `Milestone`, and `GalleryItem`.
  - Ensure entities include all fields defined in the PRD (e.g., anniversary_date, our_song_url, space_to_breathe_active).
  - Define abstract repository interfaces: `ICouplesRepository`, `IMilestonesRepository`, `IGalleryRepository`.
  - Use `Equatable` for all entities to ensure proper state management comparisons.</action>
  <verify>
    <automated>dart analyze lib/features/couples/domain</automated>
  </verify>
  <done>Domain entities and repository interfaces are defined correctly following Clean Architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Data Models & DTOs</name>
  <files>lib/features/couples/data/models/couple_bubble_model.dart, lib/features/couples/data/models/digital_hug_model.dart, lib/features/couples/data/models/heartbeat_model.dart, lib/features/couples/data/models/milestone_model.dart, lib/features/couples/data/models/gallery_item_model.dart</files>
  <action>Implement data models with JSON serialization:
  - Create `Model` classes for each entity (`CoupleBubbleModel`, etc.) extending their respective domain entities.
  - Implement `fromJson` and `toJson` methods for all models.
  - Use `json_serializable` if applicable, or manual mappers following the existing project patterns (see `lib/features/messaging/data/models/`).
  - Run `dart run build_runner build` if necessary.</action>
  <verify>
    <automated>dart analyze lib/features/couples/data/models</automated>
  </verify>
  <done>Data models with JSON serialization are implemented for all couples entities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Database and storage operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | private_gallery | mitigate | Supabase RLS policies and Storage bucket policies enforced at circle level. |
</threat_model>

<verification>
Ensure all domain entities match the PRD requirements and the Supabase migration file is valid SQL.
</verification>

<success_criteria>
- Migration file includes all 3 tables/updates and RLS.
- Domain layer strictly follows Clean Architecture (no framework/library dependencies besides Equatable).
- Data models correctly handle JSON serialization for Supabase integration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-couples/05-01-SUMMARY.md`
</output>
