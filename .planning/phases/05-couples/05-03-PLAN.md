---
phase: 05-couples
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [lib/features/couples/presentation/bloc/couple_bubble_cubit.dart, lib/features/couples/presentation/bloc/couple_bubble_state.dart, lib/features/couples/presentation/bloc/private_gallery_cubit.dart, lib/features/couples/presentation/bloc/private_gallery_state.dart, lib/features/couples/presentation/bloc/space_to_breathe_cubit.dart, lib/features/couples/presentation/bloc/space_to_breathe_state.dart]
autonomous: true
requirements: [COUP-11, COUP-12, COUP-14, COUP-15]

must_haves:
  truths:
    - "Cubit can manage real-time updates for Digital Hugs and Heartbeats"
    - "SpaceToBreathe state is managed correctly and reacts to UI actions"
    - "Private gallery state handles encrypted image list correctly"
  artifacts:
    - path: "lib/features/couples/presentation/bloc/couple_bubble_cubit.dart"
      provides: "Bubble state management and real-time triggers"
    - path: "lib/features/couples/presentation/bloc/private_gallery_cubit.dart"
      provides: "Gallery state management"
    - path: "lib/features/couples/presentation/bloc/space_to_breathe_cubit.dart"
      provides: "Pause messaging state management"
  key_links:
    - from: "lib/features/couples/presentation/bloc/couple_bubble_cubit.dart"
      to: "Supabase Realtime"
      via: "PostgreSQL inserts listen"
---

<objective>
Implement the presentation layer for Couples features, including BLoC/Cubit for the "Our Bubble" space, gallery management, and "Space to Breathe" logic with real-time integration.

Purpose: Bridge the data layer to the UI and handle real-time events for interactive couples features.
Output: State-ready BLoCs for the couples feature.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/features/couples/domain/
@lib/features/couples/data/models/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Couple Bubble Cubit & Real-time</name>
  <files>lib/features/couples/presentation/bloc/couple_bubble_cubit.dart, lib/features/couples/presentation/bloc/couple_bubble_state.dart</files>
  <action>Implement the `CoupleBubbleCubit`:
  - Fetch and manage the state of the current couple's bubble (settings, milestones, song).
  - Use `Supabase.realtime` to listen for new entries in `digital_hugs` and `heartbeats` tables for the current circle.
  - When a new hug/heartbeat arrives, emit a state update (e.g., `InteractionReceived`) to trigger UI animations.
  - Implement methods to `sendDigitalHug`, `sendHeartbeat`, and `setSharedSong`.</action>
  <verify>
    <automated>dart analyze lib/features/couples/presentation/bloc/couple_bubble_cubit.dart</automated>
  </verify>
  <done>Couple Bubble state management and real-time interactions are implemented correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Space to Breathe & Gallery Cubits</name>
  <files>lib/features/couples/presentation/bloc/private_gallery_cubit.dart, lib/features/couples/presentation/bloc/private_gallery_state.dart, lib/features/couples/presentation/bloc/space_to_breathe_cubit.dart, lib/features/couples/presentation/bloc/space_to_breathe_state.dart</files>
  <action>Implement supplementary Cubits:
  - `PrivateGalleryCubit`: Manage the list of shared private photos, handling uploads and decryption metadata.
  - `SpaceToBreatheCubit`: Manage the state of the messaging pause (activating, deactivating, and checking status).
  - Ensure both Cubits integrate with their respective repositories from 05-02.
  - Add appropriate events/states for loading, error, and success.</action>
  <verify>
    <automated>dart analyze lib/features/couples/presentation/bloc/private_gallery_cubit.dart</automated>
  </verify>
  <done>Gallery and Space to Breathe Cubits are implemented correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cubit → UI | State emission of sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | PrivateGalleryCubit state | accept | State remains in-memory within the app session. |
</threat_model>

<verification>
Ensure that the Cubits correctly emit states when repositories return data or failures.
</verification>

<success_criteria>
- Real-time listeners for hugs and heartbeats are functional.
- All Cubits properly handle loading/error states.
- State management covers all couples-specific business logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-couples/05-03-SUMMARY.md`
</output>
