---
phase: 05-couples
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [lib/features/couples/data/repositories/supabase_couples_repository.dart, lib/features/couples/data/repositories/supabase_milestones_repository.dart, lib/features/couples/data/repositories/supabase_gallery_repository.dart, lib/features/messaging/data/repositories/supabase_messaging_repository.dart]
autonomous: true
requirements: [COUP-11, COUP-12, COUP-14, COUP-15]

must_haves:
  truths:
    - "Digital Hugs and Heartbeats can be sent and persisted in Supabase"
    - "Private gallery items can be uploaded and retrieved"
    - "Messaging is paused in Couple circles when Space to Breathe is active"
  artifacts:
    - path: "lib/features/couples/data/repositories/supabase_couples_repository.dart"
      provides: "Supabase implementation for couples interactions"
    - path: "lib/features/couples/data/repositories/supabase_gallery_repository.dart"
      provides: "Encrypted gallery storage implementation"
  key_links:
    - from: "lib/features/messaging/data/repositories/supabase_messaging_repository.dart"
      to: "Supabase 'couple_bubble' table"
      via: "status check in sendMessage"
---

<objective>
Implement the data layer for Couples features, including Supabase repositories, encrypted gallery storage, and messaging pause integration.

Purpose: Connect the domain layer to real-world data sources (Supabase) and enforce feature-specific business rules like messaging pauses.
Output: Working repository implementations and messaging integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/features/couples/domain/
@lib/features/messaging/data/repositories/supabase_messaging_repository.dart
@lib/core/utils/encryption_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase Couples Repositories</name>
  <files>lib/features/couples/data/repositories/supabase_couples_repository.dart, lib/features/couples/data/repositories/supabase_milestones_repository.dart, lib/features/couples/data/repositories/supabase_gallery_repository.dart</files>
  <action>Implement the repository interfaces using `SupabaseClient`:
  - `SupabaseCouplesRepository`: Handle `couple_bubble` settings, sending `digital_hugs`, and sending `heartbeats`.
  - `SupabaseMilestonesRepository`: Fetch and add milestones for a specific circle.
  - `SupabaseGalleryRepository`: Handle image uploads to `private-gallery` storage bucket and metadata persistence.
  - Integrate `EncryptionService` for `private_gallery` captions and potentially metadata.
  - Use `injectable` for dependency injection.
  - Follow the pattern of using `Either<Failure, T>` for all methods.</action>
  <verify>
    <automated>dart analyze lib/features/couples/data/repositories</automated>
  </verify>
  <done>Supabase repositories for all couples features are implemented and pass analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Messaging Pause (Space to Breathe) Logic</name>
  <files>lib/features/messaging/data/repositories/supabase_messaging_repository.dart</files>
  <action>Update `SupabaseMessagingRepository.sendMessage` to respect "Space to Breathe" status:
  - If `message.circleId` is present, check if that circle is a `couple` circle and if `couple_bubble.space_to_breathe_active` is true.
  - If active, block the insertion of the message and return a specific `SpaceToBreatheFailure`.
  - Note: This enforces the "pause" at the data layer for consistency.
  - Use the `_client.from('couple_bubble')` to query the status based on `circle_id`.</action>
  <verify>
    <automated>dart analyze lib/features/messaging/data/repositories/supabase_messaging_repository.dart</automated>
  </verify>
  <done>Messaging is correctly paused at the repository level when Space to Breathe is active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Supabase | Transmission of feature data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | sendMessage | mitigate | Server-side status check (or RLS) to ensure messages aren't sent during pause. |
</threat_model>

<verification>
Ensure that calling `sendMessage` for a circle with active 'Space to Breathe' returns a failure.
</verification>

<success_criteria>
- All 3 repository implementations are functional.
- 'Space to Breathe' effectively blocks message sending in Couple circles.
- Gallery uploads use the correct Supabase storage bucket.
</success_criteria>

<output>
After completion, create `.planning/phases/05-couples/05-02-SUMMARY.md`
</output>
