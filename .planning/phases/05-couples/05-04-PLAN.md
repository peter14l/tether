---
phase: 05-couples
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified: [lib/features/couples/presentation/screens/our_bubble_screen.dart, lib/features/couples/presentation/widgets/interaction_animations.dart, lib/features/couples/presentation/widgets/milestone_timeline.dart, lib/features/couples/presentation/widgets/private_gallery_grid.dart, lib/features/couples/presentation/widgets/shared_song_selector.dart]
autonomous: true
requirements: [COUP-11, COUP-12, COUP-14, COUP-15]

must_haves:
  truths:
    - "User can view the 'Our Bubble' space with relationship milestones"
    - "Digital Hug and Heartbeat animations trigger correctly upon receipt"
    - "User can set a shared song and view the private gallery"
  artifacts:
    - path: "lib/features/couples/presentation/screens/our_bubble_screen.dart"
      provides: "Main private space UI"
    - path: "lib/features/couples/presentation/widgets/interaction_animations.dart"
      provides: "Pulse and ripple animations"
  key_links:
    - from: "lib/features/couples/presentation/screens/our_bubble_screen.dart"
      to: "lib/features/couples/presentation/bloc/couple_bubble_cubit.dart"
      via: "BlocBuilder/BlocListener"
---

<objective>
Implement the UI for the "Our Bubble" space, including interactive animations for Digital Hugs and Heartbeats, a shared gallery, and a milestone timeline.

Purpose: Provide the visual and interactive experience for the couples feature.
Output: A complete and functional "Our Bubble" screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/features/couples/presentation/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Our Bubble Screen & Animations</name>
  <files>lib/features/couples/presentation/screens/our_bubble_screen.dart, lib/features/couples/presentation/widgets/interaction_animations.dart, lib/features/couples/presentation/widgets/shared_song_selector.dart</files>
  <action>Build the main bubble UI:
  - Implement `OurBubbleScreen` with a warm, soft gradient background (PRD 7.3).
  - Add buttons for `Heartbeat` and `Digital Hug`.
  - Implement `DigitalHugAnimation` (radial pulse expand + fade) and `HeartbeatAnimation` (concentric ripple).
  - Use `BlocListener` in `OurBubbleScreen` to trigger these animations when a `InteractionReceived` state is emitted by `CoupleBubbleCubit`.
  - Implement a `SharedSongSelector` widget to update the bubble's song (using simple text/URL inputs for MVP).</action>
  <verify>
    <automated>dart analyze lib/features/couples/presentation/screens/our_bubble_screen.dart</automated>
  </verify>
  <done>Our Bubble screen with interactive animations and shared song selection is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery & Milestones UI</name>
  <files>lib/features/couples/presentation/widgets/milestone_timeline.dart, lib/features/couples/presentation/widgets/private_gallery_grid.dart</files>
  <action>Implement the visual history widgets:
  - `MilestoneTimeline`: A vertical, warm timeline showing relationship milestones (auto and manual).
  - `PrivateGalleryGrid`: A grid view of photos from the `private_gallery` table.
  - Apply the "Slow Photo" fade-in (Feature 25) using `FadeInImage` or similar pattern for all gallery items.
  - Add an "Add Milestone" and "Upload Photo" UI triggers.</action>
  <verify>
    <automated>dart analyze lib/features/couples/presentation/widgets/milestone_timeline.dart</automated>
  </verify>
  <done>Gallery grid and Milestone timeline are implemented and integrated into the Bubble screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Storage | Fetching signed URLs for gallery images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | Gallery display | mitigate | Ensure image URLs are signed with short-lived expiry (Supabase storage default). |
</threat_model>

<verification>
Manually verify that sending a heartbeat from one account triggers the animation on the partner's screen (requires two sessions).
</verification>

<success_criteria>
- The UI matches the calm design philosophy of Tether.
- Animations are smooth and non-jarring.
- Milestones and gallery correctly display shared content.
- 'Our Song' is visible and updatable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-couples/05-04-SUMMARY.md`
</output>
