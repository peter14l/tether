---
phase: 02-circles-feed
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [lib/core/theme/theme_tokens.dart, lib/core/theme/app_theme.dart]
autonomous: true
requirements: [UI-TA-01, REQ-UI-05]
must_haves:
  truths:
    - "ThemeTokens contains glowShadows and textOnAccent for all time slots"
    - "AppTheme applies global component themes for Buttons, Inputs, and Cards"
  artifacts:
    - path: "lib/core/theme/theme_tokens.dart"
      provides: "Glow shadow and text contrast tokens"
    - path: "lib/core/theme/app_theme.dart"
      provides: "Centralized component styling"
  key_links:
    - from: "lib/core/theme/app_theme.dart"
      to: "lib/core/theme/theme_tokens.dart"
      via: "tokens variable"
---

<objective>
Update the core theme foundation to support the "glow" aesthetic and standardized component styling across the entire application.

Purpose: Ensure visual consistency and brand-aligned "glow" effects without hardcoding styles in individual screens.
Output: Enhanced ThemeTokens and AppTheme with centralized component themes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-circles-feed/02-06-RESEARCH.md
@.planning/phases/02-circles-feed/02-UI-REVIEW.md
@lib/core/theme/theme_tokens.dart
@lib/core/theme/app_theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ThemeTokens with Glow and Contrast tokens</name>
  <files>lib/core/theme/theme_tokens.dart</files>
  <action>
    - Add `final List<BoxShadow> glowShadows;` and `final Color textOnAccent;` fields to the `ThemeTokens` class.
    - Update the constructor and all static instances (`morning`, `afternoon`, `dusk`, `night`).
    - Use the multi-layered shadow stack suggested in 02-06-RESEARCH.md for `dusk` and `night` glow shadows.
    - Use a single, subtle shadow for `morning` and `afternoon`.
    - Set `textOnAccent` to provide optimal contrast against `accentPrimary` for each time slot (e.g., white for dusk, dark for morning).
  </action>
  <verify>
    <automated>grep -q "glowShadows" lib/core/theme/theme_tokens.dart && grep -q "textOnAccent" lib/core/theme/theme_tokens.dart</automated>
  </verify>
  <done>ThemeTokens includes glowShadows and textOnAccent fields, and all presets are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AppTheme with Global Component Themes</name>
  <files>lib/core/theme/app_theme.dart</files>
  <action>
    - Update `getTheme` to include `elevatedButtonTheme`, `outlinedButtonTheme`, and `inputDecorationTheme`.
    - Move button styles from `lib/features/circles/presentation/screens/create_circle_screen.dart` into `AppTheme` to use 14px radius and standard padding (16/28).
    - Set `InputDecorationTheme` to use 14px radius, `backgroundSecondary` as fill color, and `borderDefault` for the border.
    - Update `CardTheme` to use `glowShadows` for `dusk` and `night` instead of just elevation (if achievable via `CardTheme.shadowColor`, otherwise ensure it's prepared for TetherCard).
    - Ensure `onPrimary` and `onSecondary` in `ColorScheme` match the new `textOnAccent` tokens.
  </action>
  <verify>
    <automated>grep -q "elevatedButtonTheme" lib/core/theme/app_theme.dart && grep -q "inputDecorationTheme" lib/core/theme/app_theme.dart</automated>
  </verify>
  <done>AppTheme contains centralized styling for buttons, inputs, and cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Theme | Theme tokens are trusted internal constants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | theme_tokens.dart | accept | No sensitive data in theme tokens. |
| T-02-06-02 | Denial of Service | AppTheme | mitigate | Avoid complex canvas operations or infinite loops in theme calculation. Standard ThemeData is performant. |
</threat_model>

<verification>
Run `flutter test` to ensure theme changes don't break existing widget tests (if any).
Verify that the app compiles and the theme tokens are accessible.
</verification>

<success_criteria>
- ThemeTokens class updated with new fields.
- All four TimeSlot presets in ThemeTokens updated with glowShadows and textOnAccent.
- AppTheme refactored to include global button and input themes.
- No regressions in existing UI layouts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-06-SUMMARY.md`
</output>
