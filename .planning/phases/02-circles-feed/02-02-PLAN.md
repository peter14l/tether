---
phase: 02-circles-feed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  lib/features/feed/domain/entities/post_entity.dart,
  lib/features/feed/domain/entities/reaction_entity.dart,
  lib/features/feed/data/models/post_model.dart,
  lib/features/feed/data/models/reaction_model.dart,
  lib/features/feed/domain/repositories/feed_repository.dart,
  lib/features/feed/data/repositories/supabase_feed_repository.dart,
  lib/injection_container.dart
]
autonomous: true
requirements: [FEED-03]

must_haves:
  truths:
    - "Post and Reaction entities represent the Supabase schema correctly"
    - "Feed repository can fetch and create posts and reactions in Supabase"
  artifacts:
    - path: "lib/features/feed/domain/entities/post_entity.dart"
      provides: "Post domain entity"
    - path: "lib/features/feed/data/repositories/supabase_feed_repository.dart"
      provides: "Supabase implementation for Feed repository"
  key_links:
    - from: "lib/features/feed/data/repositories/supabase_feed_repository.dart"
      to: "SupabaseClient"
      via: "Dependency Injection"
---

<objective>
Implement the Domain and Data layers for Circle Feed management using Clean Architecture.
This includes the entities, models, and Supabase-backed repository for posts and reactions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TETHER_PRD.md
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Post and Reaction Entities and Models</name>
  <files>
    lib/features/feed/domain/entities/post_entity.dart,
    lib/features/feed/domain/entities/reaction_entity.dart,
    lib/features/feed/data/models/post_model.dart,
    lib/features/feed/data/models/reaction_model.dart
  </files>
  <action>
    Create immutable entities for Post and Reaction.
    Then create Models that include JSON serialization.
    Match 'posts' and 'reactions' tables:
    - Post: id, circle_id, author_id, content_type, content_text, media_url, is_anonymous, deliver_at, expires_after, is_soft_deleted, created_at.
    - Reaction: id, post_id, user_id, reaction_type, created_at.
    Post content_type must be 'text' | 'image' | 'voice' | 'letter' | 'one_way'.
    Reaction types must be 'warm' | 'comforting' | 'i_see_you' | 'sending_strength'.
  </action>
  <verify>
    <automated>dart analyze lib/features/feed/data/models/</automated>
  </verify>
  <done>Entities and Models exist with JSON support and match schema.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SupabaseFeedRepository</name>
  <files>
    lib/features/feed/domain/repositories/feed_repository.dart,
    lib/features/feed/data/repositories/supabase_feed_repository.dart,
    lib/injection_container.dart
  </files>
  <action>
    1. Define 'IFeedRepository' with methods:
       - Future<Either<Failure, List<Post>>> getCircleFeed(String circleId)
       - Future<Either<Failure, Post>> createPost(Post post)
       - Future<Either<Failure, void>> addReaction(String postId, String reactionType)
       - Future<Either<Failure, void>> removeReaction(String postId)
    2. Implement 'SupabaseFeedRepository' using SupabaseClient.
    3. Register 'IFeedRepository' in 'injection_container.dart'.
  </action>
  <verify>
    <automated>dart analyze lib/features/feed/data/repositories/</automated>
  </verify>
  <done>Repository implemented and registered in DI container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Flutter App → Supabase API | Data sent over HTTPS. Supabase RLS enforces permissions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Post creation | mitigate | Enforce RLS: author_id must match auth.uid() |
| T-02-04 | Information Disclosure | Post listing | mitigate | Enforce RLS: user must be in circle_members |
</threat_model>

<verification>
Verify repository methods interact correctly with SupabaseClient.
</verification>

<success_criteria>
- Models match Supabase schema exactly.
- Feed repository supports posts and reactions.
- Repository is registered and injectable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-02-SUMMARY.md`
</output>
