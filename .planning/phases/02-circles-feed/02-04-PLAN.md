---
phase: 02-circles-feed
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-03]
files_modified: [
  lib/features/circles/presentation/bloc/circle_cubit.dart,
  lib/features/circles/presentation/screens/circles_screen.dart,
  lib/features/circles/presentation/screens/create_circle_screen.dart,
  lib/core/router/app_router.dart
]
autonomous: false
requirements: [CIRCLES-02]

must_haves:
  truths:
    - "User can view their circles in a list"
    - "User can create new Friend, Couple, and Family circles"
  artifacts:
    - path: "lib/features/circles/presentation/bloc/circle_cubit.dart"
      provides: "Circle state management"
    - path: "lib/features/circles/presentation/screens/create_circle_screen.dart"
      provides: "UI for creating new circles"
  key_links:
    - from: "lib/features/circles/presentation/bloc/circle_cubit.dart"
      to: "ICircleRepository"
      via: "Dependency Injection"
---

<objective>
Implement the presentation layer and UI for Circle management.
Users should be able to see their circles and create new ones of different types.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@.planning/phases/02-circles-feed/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CircleCubit</name>
  <files>lib/features/circles/presentation/bloc/circle_cubit.dart</files>
  <action>
    Create 'CircleCubit' and 'CircleState'.
    State should include: list of circles, loading status, and error messages.
    Methods:
    - 'loadCircles()': Fetches circles from ICircleRepository.
    - 'createCircle(String name, String type)': Calls repository to create a circle.
  </action>
  <verify>
    <automated>dart analyze lib/features/circles/presentation/bloc/</automated>
  </verify>
  <done>Cubit handles business logic for circle management.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Build Circles and Create Circle Screens</name>
  <files>
    lib/features/circles/presentation/screens/circles_screen.dart,
    lib/features/circles/presentation/screens/create_circle_screen.dart,
    lib/core/router/app_router.dart
  </files>
  <action>
    1. Create 'CirclesScreen' showing a list of circles (use card components).
    2. Create 'CreateCircleScreen' with a form to name the circle and select its type (Friend, Couple, Family).
    3. Update 'app_router.dart' to include routes for these screens.
    4. Implement logic to restrict Couple circles to exactly 2 members in the UI flow.
  </action>
  <what-built>Circle list and creation UI screens.</what-built>
  <how-to-verify>
    1. Navigate to the circles list.
    2. Tap '+' to open create circle screen.
    3. Create a circle and verify it appears in the list.
  </how-to-verify>
  <done>User can create and view circles in the app UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User UI → CircleCubit | Direct interaction with BLoC. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | Circle creation | mitigate | Implement basic client-side input validation for names. |
</threat_model>

<verification>
Manual verification of UI flow and circle creation success.
</verification>

<success_criteria>
- Circle list shows data from Supabase.
- User can select between Friend/Couple/Family types.
- Navigation works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-04-SUMMARY.md`
</output>
