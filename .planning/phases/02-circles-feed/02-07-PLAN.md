---
phase: 02-circles-feed
plan: 07
type: execute
wave: 2
depends_on: [02-06]
files_modified: [lib/core/widgets/tether_button.dart, lib/core/widgets/slow_photo.dart, lib/core/widgets/tether_card.dart, lib/core/widgets/tether_text_field.dart]
autonomous: true
requirements: [REQ-UI-05, UI-GLOW-01, UI-PHOTO-01, UI-FIELD-01]
must_haves:
  truths:
    - "TetherButton implements the multi-layered glow shadow from ThemeTokens"
    - "SlowPhoto provides a 600ms ease-in opacity transition for network images"
    - "TetherCard supports dusk-specific inner glows via decoration"
    - "TetherTextField implements custom border, padding, and dusk-mode glow"
  artifacts:
    - path: "lib/core/widgets/tether_button.dart"
      provides: "Reusable button with glow branding and accessibility support"
    - path: "lib/core/widgets/slow_photo.dart"
      provides: "Aesthetic image loading widget"
    - path: "lib/core/widgets/tether_card.dart"
      provides: "Aesthetic card container with inner glow support"
    - path: "lib/core/widgets/tether_text_field.dart"
      provides: "Aesthetic text input with glow and custom borders"
  key_links:
    - from: "lib/core/widgets/tether_button.dart"
      to: "lib/core/theme/theme_tokens.dart"
      via: "tokens.glowShadows"
---

<objective>
Implement the "Aesthetic Primitive" widgets that define the brand's visual identity, focusing on glow effects, soft motion, and inclusive design.

Purpose: Provide standardized, reusable components that encapsulate the complex "glow" logic and motion specifications.
Output: TetherButton, SlowPhoto, TetherCard, and TetherTextField widgets in core.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-circles-feed/02-06-RESEARCH.md
@lib/core/theme/theme_tokens.dart
@lib/core/theme/app_theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TetherButton and TetherCard</name>
  <files>lib/core/widgets/tether_button.dart, lib/core/widgets/tether_card.dart</files>
  <action>
    - Create `lib/core/widgets/tether_button.dart` as a wrapper around `ElevatedButton`.
    - It should use a `Container` with `BoxDecoration` to apply `ThemeTokens.getTokens(slot).glowShadows` when the app is in Dusk or Night mode.
    - Ensure `TetherButton` includes accessibility features (built-in `Tooltip` and `Semantics`).
    - Create `lib/core/widgets/tether_card.dart` as a wrapper around `Card` or a `Container`.
    - Implement the "top-edge inner glow" for Dusk mode using a `LinearGradient` or subtle `Border` as suggested in research.
    - Ensure both widgets respect the current `TimeSlot` from the theme.
  </action>
  <verify>
    <automated>ls lib/core/widgets/tether_button.dart && ls lib/core/widgets/tether_card.dart</automated>
  </verify>
  <done>Standardized button and card widgets with glow effects and accessibility are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SlowPhoto Widget</name>
  <files>lib/core/widgets/slow_photo.dart</files>
  <action>
    - Create `lib/core/widgets/slow_photo.dart` using `Image.network`.
    - Implement the `frameBuilder` to provide a 600ms `AnimatedOpacity` transition (ease-in) once the image is loaded.
    - Include support for a placeholder/shimmer (using `backgroundSecondary`) while loading.
    - Ensure it handles errors gracefully with a fallback icon.
  </action>
  <verify>
    <automated>grep -q "AnimatedOpacity" lib/core/widgets/slow_photo.dart && grep -q "Duration(milliseconds: 600)" lib/core/widgets/slow_photo.dart</automated>
  </verify>
  <done>SlowPhoto widget provides a 600ms fade-in transition for all network images.</done>
</task>

<task type="auto">
  <name>Task 3: Implement TetherTextField Widget</name>
  <files>lib/core/widgets/tether_text_field.dart</files>
  <action>
    - Create `lib/core/widgets/tether_text_field.dart` as a custom wrapper for `TextField`.
    - Design it for high-aesthetic use (like Chat): custom border decoration, subtle inner padding (e.g., 16-20px), and optional glow (BoxShadow) during focus or in dark time slots.
    - Ensure it supports standard `TextEditingController` and `InputDecoration` properties.
  </action>
  <verify>
    <automated>ls lib/core/widgets/tether_text_field.dart</automated>
  </verify>
  <done>TetherTextField implemented with custom aesthetic refinement (glow/border/padding).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> SlowPhoto | External image data is untrusted. |
| User -> TetherTextField | User input in text fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Denial of Service | SlowPhoto | mitigate | Use cached_network_image or standard Flutter Image caching. Limit image dimensions if possible. |
| T-02-07-02 | Information Disclosure | SlowPhoto | accept | Public image URLs only; no PII in requests. |
| T-02-07-03 | Injection | TetherTextField | mitigate | Standard sanitization by the features consuming the widget. |
</threat_model>

<verification>
Create a temporary test page or widget test to verify the widgets render correctly and apply shadows/transitions as expected.
Perform visual audits for all newly created primitives in various TimeSlots.
</verification>

<success_criteria>
- TetherButton, TetherCard, SlowPhoto, and TetherTextField files exist in lib/core/widgets/.
- TetherButton applies multi-layered shadows and includes accessibility tags.
- SlowPhoto implements the 600ms ease-in opacity transition.
- TetherTextField has custom border and padding specifications.
- Widgets pass visual audit for "glow" aesthetic across all themes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-07-SUMMARY.md`
</output>
