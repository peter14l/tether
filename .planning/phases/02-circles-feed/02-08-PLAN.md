---
phase: 02-circles-feed
plan: 08
type: execute
wave: 3
depends_on: [02-07]
files_modified: [lib/features/feed/presentation/widgets/post_card.dart, lib/features/feed/presentation/screens/feed_screen.dart, lib/features/circles/presentation/screens/create_circle_screen.dart, lib/features/circles/presentation/screens/circles_screen.dart, lib/features/circles/presentation/widgets/circle_card.dart, lib/features/circles/presentation/widgets/member_card.dart, lib/features/messaging/presentation/screens/chat_screen.dart]
autonomous: false
requirements: [REQ-ACC-01, UI-POLISH-01]
must_haves:
  truths:
    - "All icon-only buttons have tooltips and semantics"
    - "Standard Cards on Circles screen are replaced with CircleCard and MemberCard (using TetherCard)"
    - "Chat screen TextField is replaced with refined TetherTextField"
    - "Network images in all features use SlowPhoto for smooth loading"
    - "All features (Feed, Circles, Chat, Create Circle) use TetherButton for primary actions"
  artifacts:
    - path: "lib/features/circles/presentation/widgets/circle_card.dart"
      provides: "Custom card for Circle list items"
    - path: "lib/features/circles/presentation/widgets/member_card.dart"
      provides: "Custom card for Member list items"
  key_links:
    - from: "lib/features/circles/presentation/screens/circles_screen.dart"
      to: "lib/features/circles/presentation/widgets/circle_card.dart"
      via: "CircleCard widget"
    - from: "lib/features/messaging/presentation/screens/chat_screen.dart"
      to: "lib/core/widgets/tether_text_field.dart"
      via: "TetherTextField widget"
---

<objective>
Apply aesthetic primitives and accessibility improvements across all existing features (Feed, Circles, Chat, Messaging) to ensure a cohesive, high-quality, and inclusive brand experience.

Purpose: Consistency, inclusive design, and brand polish. Ensure the "glow" and "motion" philosophy is felt everywhere.
Output: Refined and accessible screens across the app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-circles-feed/02-UI-REVIEW.md
@lib/core/widgets/tether_button.dart
@lib/core/widgets/slow_photo.dart
@lib/core/widgets/tether_card.dart
@lib/core/widgets/tether_text_field.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine Circles and Feed Aesthetics</name>
  <files>lib/features/circles/presentation/screens/circles_screen.dart, lib/features/circles/presentation/widgets/circle_card.dart, lib/features/circles/presentation/widgets/member_card.dart, lib/features/feed/presentation/widgets/post_card.dart, lib/features/feed/presentation/screens/feed_screen.dart</files>
  <action>
    - Create `lib/features/circles/presentation/widgets/circle_card.dart` and `member_card.dart`.
    - Both widgets must use `TetherCard` as their base container.
    - Replace standard `Card` and `ListTile` usages in `circles_screen.dart` with `CircleCard` and `MemberCard` (per D-XX).
    - Replace all `Image.network` usages in `post_card.dart` with the `SlowPhoto` widget.
    - Replace standard `Card` in `post_card.dart` with `TetherCard`.
    - Ensure all primary buttons in these screens use `TetherButton`.
  </action>
  <verify>
    <automated>grep -q "TetherCard" lib/features/circles/presentation/widgets/circle_card.dart && grep -q "SlowPhoto" lib/features/feed/presentation/widgets/post_card.dart</automated>
  </verify>
  <done>Circles and Feed features now use Tether aesthetic primitives (TetherCard, SlowPhoto, TetherButton).</done>
</task>

<task type="auto">
  <name>Task 2: Refine Chat and Messaging Aesthetics</name>
  <files>lib/features/messaging/presentation/screens/chat_screen.dart, lib/features/circles/presentation/screens/create_circle_screen.dart</files>
  <action>
    - In `chat_screen.dart`, replace the standard `TextField` with `TetherTextField`.
    - Apply the Chat screen specific refinements: ensure the custom border, glow, and padding from `TetherTextField` are correctly configured for the chat input.
    - Replace `ElevatedButton` or standard `IconButton` (if used for primary actions) with `TetherButton` in `create_circle_screen.dart` and `chat_screen.dart`.
    - Ensure `SlowPhoto` is used for any avatars or media in the chat.
  </action>
  <verify>
    <automated>grep -q "TetherTextField" lib/features/messaging/presentation/screens/chat_screen.dart</automated>
  </verify>
  <done>Chat and Messaging features now use Tether aesthetic primitives and refined input fields.</done>
</task>

<task type="auto">
  <name>Task 3: Global Accessibility Improvements</name>
  <files>lib/features/messaging/presentation/screens/chat_screen.dart, lib/features/feed/presentation/screens/feed_screen.dart, lib/features/circles/presentation/screens/circles_screen.dart</files>
  <action>
    - Audit all `IconButton` and custom button usages across these features.
    - Wrap every `IconButton` that doesn't have a nearby text label with a `Tooltip`.
    - Add `Semantics(label: "...", child: ...)` tags to all newly created or updated buttons.
    - Verify that `TetherButton` correctly exposes its accessibility properties.
    - Specific focus on: "Add Circle" FAB, "Send" icons, "Back" icons, and "Reaction" buttons.
  </action>
  <verify>
    <automated>grep -c "Tooltip" lib/features/feed/presentation/screens/feed_screen.dart</automated>
  </verify>
  <done>All buttons across Feed, Circles, and Chat are accessible with tooltips and semantic labels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual Audit and Accessibility Verification</name>
  <files>N/A</files>
  <action>Perform visual audit and accessibility verification as described below.</action>
  <what-built>Refined UI with Glows, Motion, and Accessibility across all core features.</what-built>
  <how-to-verify>
    1. Run the app in a simulator/device.
    2. Visit the Feed: observe `SlowPhoto` fade-in and `TetherCard` glow/border.
    3. Visit Your Circles: verify `CircleCard` and `MemberCard` are used and look consistent with the brand.
    4. Open a Chat: verify the refined `TetherTextField` (custom border, glow, padding) and accessible "Send" button.
    5. Test Dusk/Night themes: verify multi-layered glows on all `TetherCard` and `TetherButton` instances.
    6. Long-press on any icon-only button to verify tooltips appear.
  </how-to-verify>
  <verify>Human approval after visual inspection.</verify>
  <done>UI passes visual audit for "glow" aesthetic consistency and accessibility.</done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | User interaction with buttons and forms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Accessibility Obstruction | App-wide | mitigate | Regular use of semantics and tooltips. Use accessibility tools during manual verification. |
| T-02-08-02 | Visual Regression | App-wide | mitigate | Comprehensive visual audits across all affected screens and widget tests. |
</threat_model>

<verification>
Run `flutter test` to ensure all screens still render correctly.
Perform a full visual audit of all affected screens (Feed, Circles, Chat, Messaging, Create Circle) at different "TimeSlots" to ensure glow and motion consistency.
</verification>

<success_criteria>
- No standard Card or TextField in primary feature screens.
- All network images use SlowPhoto.
- All buttons include tooltips and semantics.
- UI passes visual audit for "glow" aesthetic consistency across all core features.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-08-SUMMARY.md`
</output>
