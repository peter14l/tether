---
phase: 02-circles-feed
plan: 08
type: execute
wave: 3
depends_on: [02-07]
files_modified: [lib/features/feed/presentation/widgets/post_card.dart, lib/features/feed/presentation/screens/feed_screen.dart, lib/features/circles/presentation/screens/create_circle_screen.dart, lib/features/circles/presentation/screens/circles_screen.dart, lib/features/messaging/presentation/screens/chat_screen.dart]
autonomous: false
requirements: [REQ-ACC-01, UI-POLISH-01]
must_haves:
  truths:
    - "All icon-only buttons have tooltips and semantics"
    - "ElevatedButtons are replaced with TetherButton or use global theme without hardcoded styles"
    - "Network images in PostCards use SlowPhoto for smooth loading"
  artifacts:
    - path: "lib/features/feed/presentation/widgets/post_card.dart"
      provides: "Aesthetic feed items"
  key_links:
    - from: "lib/features/feed/presentation/widgets/post_card.dart"
      to: "lib/core/widgets/slow_photo.dart"
      via: "SlowPhoto widget"
---

<objective>
Apply aesthetic refinements and accessibility improvements across all existing features to ensure a cohesive, high-quality user experience.

Purpose: Consistency and inclusive design. Ensure the brand-new glow and motion philosophy is felt everywhere.
Output: Refined and accessible screens across the app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-circles-feed/02-UI-REVIEW.md
@lib/core/widgets/tether_button.dart
@lib/core/widgets/slow_photo.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Aesthetic Refinement</name>
  <files>lib/features/feed/presentation/widgets/post_card.dart, lib/features/feed/presentation/screens/feed_screen.dart, lib/features/circles/presentation/screens/create_circle_screen.dart, lib/features/circles/presentation/screens/circles_screen.dart</files>
  <action>
    - Replace all `Image.network` usages in `post_card.dart` with the `SlowPhoto` widget.
    - Replace `Card` in `post_card.dart` with `TetherCard`.
    - In `create_circle_screen.dart` and `circles_screen.dart`, replace `ElevatedButton` with `TetherButton` or remove the local `styleFrom` calls (allowing the global `AppTheme` to take over).
    - Ensure consistent 14px radius on all inputs and buttons.
  </action>
  <verify>
    <automated>grep -q "SlowPhoto" lib/features/feed/presentation/widgets/post_card.dart && grep -v "styleFrom" lib/features/circles/presentation/screens/create_circle_screen.dart</automated>
  </verify>
  <done>Existing features now use the aesthetic primitives and global component themes.</done>
</task>

<task type="auto">
  <name>Task 2: Global Accessibility Improvements</name>
  <files>lib/features/messaging/presentation/screens/chat_screen.dart, lib/features/feed/presentation/screens/feed_screen.dart, lib/features/circles/presentation/screens/circles_screen.dart</files>
  <action>
    - Audit all `IconButton` usages.
    - Wrap every `IconButton` that doesn't have a nearby text label with a `Tooltip`.
    - Add `Semantics(label: "...", child: IconButton(...))` or ensure `IconButton.tooltip` is set to provide accessibility.
    - Specific focus on: "Add Circle" FAB, "Send" icons, "Back" icons, and "Reaction" buttons.
  </action>
  <verify>
    <automated>grep -c "Tooltip" lib/features/feed/presentation/screens/feed_screen.dart</automated>
  </verify>
  <done>All icon buttons are accessible with tooltips and semantic labels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refined UI with Glows, Motion, and Accessibility across the app.</what-built>
  <how-to-verify>
    1. Run the app in a simulator/device.
    2. Visit the Feed and observe the "Slow Photo" fade-in when images load.
    3. Check the "Dusk" and "Night" themes — verify that buttons and cards have a multi-layered glow.
    4. Long-press on any icon-only button to verify tooltips appear.
    5. Check the "Create Circle" form — verify the input fields have the correct radius and colors.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | User interaction with buttons and forms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Accessibility Obstruction | App-wide | mitigate | Regular use of semantics and tooltips. Use accessibility tools during manual verification. |
| T-02-08-02 | Visual Regression | App-wide | mitigate | Comprehensive visual audit and widget tests. |
</threat_model>

<verification>
Run `flutter test` to ensure all screens still render correctly.
Perform a visual audit of the app at different "TimeSlots" (Time-Adaptive UI).
</verification>

<success_criteria>
- No hardcoded styleFrom calls for standard buttons.
- All network images use SlowPhoto.
- No icon-only buttons without tooltips.
- UI passes visual audit for "glow" aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-08-SUMMARY.md`
</output>
