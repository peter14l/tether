---
phase: 02-circles-feed
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [
  lib/core/theme/time_theme_cubit.dart,
  lib/core/theme/time_theme_state.dart,
  lib/core/theme/theme_tokens.dart,
  lib/core/theme/app_theme.dart,
  lib/main.dart
]
autonomous: true
requirements: [TIME-UI-05]

must_haves:
  truths:
    - "App theme changes based on the time of day automatically"
    - "Color palettes for Morning, Afternoon, Dusk, and Night match PRD 4.1"
  artifacts:
    - path: "lib/core/theme/theme_tokens.dart"
      provides: "Color and typography tokens for each time slot"
    - path: "lib/core/theme/time_theme_cubit.dart"
      provides: "State management for time slots"
  key_links:
    - from: "lib/main.dart"
      to: "TimeThemeCubit"
      via: "BlocBuilder/BlocProvider"
---

<objective>
Implement the Time-Adaptive UI Engine that shifts the app's color theme dynamically based on the device's local time.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Time Slots and Theme Tokens</name>
  <files>
    lib/core/theme/theme_tokens.dart,
    lib/core/theme/time_theme_state.dart
  </files>
  <action>
    1. Define 'TimeSlot' enum: morning, afternoon, dusk, night.
    2. Define 'ThemeTokens' class containing color palettes for each slot exactly as specified in PRD 4.1.
       - Morning (5:00 - 11:59): Fresh, warm linen white, soft terracotta.
       - Afternoon (12:00 - 17:29): Bright, cream, golden amber.
       - Dusk (17:30 - 20:59): Deep plum, vivid sunset orange.
       - Night (21:00 - 4:59): Midnight navy, periwinkle.
    3. Include typography tokens (Playfair Display for headings, DM Sans for body).
  </action>
  <verify>
    <automated>dart analyze lib/core/theme/</automated>
  </verify>
  <done>Tokens and slots defined according to PRD.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TimeThemeCubit</name>
  <files>
    lib/core/theme/time_theme_cubit.dart,
    lib/core/theme/app_theme.dart
  </files>
  <action>
    1. Create 'TimeThemeCubit' that:
       - Calculates the current 'TimeSlot' on init.
       - Sets up a Timer to check for slot changes every minute.
       - Emits 'TimeThemeState' with the current slot.
    2. Implement 'AppTheme' class with a static 'getTheme(TimeSlot slot)' method that returns 'ThemeData' built from the corresponding tokens.
    3. Use 'AnimatedTheme' transition logic or ensure 'ThemeData' is properly interpolated if possible.
  </action>
  <verify>
    <automated>dart analyze lib/core/theme/</automated>
  </verify>
  <done>Cubit manages time transitions and provides ThemeData.</done>
</task>

<task type="auto">
  <name>Task 3: Wire TimeThemeCubit to MaterialApp</name>
  <files>lib/main.dart</files>
  <action>
    Wrap 'MaterialApp' with a 'BlocProvider' for 'TimeThemeCubit' and use 'BlocBuilder' to update the 'theme' property based on the current state.
    Ensure 'GoRouter' is still correctly integrated.
  </action>
  <verify>
    <automated>dart analyze lib/main.dart</automated>
  </verify>
  <done>App now reacts to time changes by updating the global theme.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| System Clock → App Logic | App trusts the device time for theme selection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Device Time | accept | Theme change is purely visual/non-critical. |
</threat_model>

<verification>
Change the system time manually on the emulator/device to verify theme transitions (Morning, Afternoon, Dusk, Night).
</verification>

<success_criteria>
- UI colors shift automatically based on device time.
- 4 distinct themes implemented per PRD specs.
- Transitions are smooth and handled via Cubit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-03-SUMMARY.md`
</output>
