---
phase: 02-circles-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  lib/features/circles/domain/entities/circle_entity.dart,
  lib/features/circles/domain/entities/circle_member_entity.dart,
  lib/features/circles/data/models/circle_model.dart,
  lib/features/circles/data/models/circle_member_model.dart,
  lib/features/circles/domain/repositories/circle_repository.dart,
  lib/features/circles/data/repositories/supabase_circle_repository.dart,
  lib/injection_container.dart
]
autonomous: true
requirements: [CIRCLES-02]

must_haves:
  truths:
    - "Circle and Member entities represent the Supabase schema correctly"
    - "Circle repository can fetch and create circles in Supabase"
  artifacts:
    - path: "lib/features/circles/domain/entities/circle_entity.dart"
      provides: "Circle domain entity"
    - path: "lib/features/circles/data/repositories/supabase_circle_repository.dart"
      provides: "Supabase implementation for Circle repository"
  key_links:
    - from: "lib/features/circles/data/repositories/supabase_circle_repository.dart"
      to: "SupabaseClient"
      via: "Dependency Injection"
---

<objective>
Implement the Domain and Data layers for Circle management using Clean Architecture.
This includes the entities, models, and Supabase-backed repository.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TETHER_PRD.md
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Circle and CircleMember Entities and Models</name>
  <files>
    lib/features/circles/domain/entities/circle_entity.dart,
    lib/features/circles/domain/entities/circle_member_entity.dart,
    lib/features/circles/data/models/circle_model.dart,
    lib/features/circles/data/models/circle_member_model.dart
  </files>
  <action>
    Create immutable entities for Circle and CircleMember.
    Then create Models that extend these entities and include JSON serialization (using equatable and json_serializable/manual mappers).
    Ensure fields match the 'circles' and 'circle_members' tables in the schema:
    - Circle: id, name, circle_type, created_by, avatar_url, description, comfort_radius, is_encrypted.
    - CircleMember: id, circle_id, user_id, role, joined_at.
    Circle types must be 'friends' | 'couple' | 'family' | 'inlaw'.
  </action>
  <verify>
    <automated>dart analyze lib/features/circles/data/models/</automated>
  </verify>
  <done>Entities and Models exist with JSON support and match schema.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SupabaseCircleRepository</name>
  <files>
    lib/features/circles/domain/repositories/circle_repository.dart,
    lib/features/circles/data/repositories/supabase_circle_repository.dart,
    lib/injection_container.dart
  </files>
  <action>
    1. Define 'ICircleRepository' with methods:
       - Future<Either<Failure, List<Circle>>> getMyCircles()
       - Future<Either<Failure, Circle>> createCircle(Circle circle)
       - Future<Either<Failure, void>> addMember(String circleId, String userId, String role)
    2. Implement 'SupabaseCircleRepository' using SupabaseClient.
    3. Register 'ICircleRepository' in 'injection_container.dart' as a LazySingleton using '@LazySingleton(as: ICircleRepository)'.
  </action>
  <verify>
    <automated>dart analyze lib/features/circles/data/repositories/</automated>
  </verify>
  <done>Repository implemented and registered in DI container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Flutter App → Supabase API | Data sent over HTTPS. Supabase RLS enforces permissions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Circle creation | mitigate | Enforce RLS: created_by must match auth.uid() |
| T-02-02 | Information Disclosure | Circle listing | mitigate | Enforce RLS: user must be in circle_members |
</threat_model>

<verification>
Check that the repository methods correctly interact with Supabase client (can be mock-tested or verified via code review of RLS matching).
</verification>

<success_criteria>
- Models match Supabase schema exactly.
- Repository is registered and injectable.
- RLS policies (from schema) are accounted for in data access.
</success_criteria>

<output>
After completion, create `.planning/phases/02-circles-feed/02-01-SUMMARY.md`
</output>
