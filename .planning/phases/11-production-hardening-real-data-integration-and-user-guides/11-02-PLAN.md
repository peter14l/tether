---
wave: 2
depends_on: [11-01]
files_modified:
  - lib/features/feed/data/repositories/supabase_feed_repository.dart
  - lib/features/feed/presentation/bloc/feed_cubit.dart
  - lib/features/messaging/data/repositories/supabase_messaging_repository.dart
  - lib/features/messaging/presentation/blocs/messaging_bloc.dart
autonomous: true
---

# Phase 11, Plan 02: Real Data Integration (Feed & Realtime Core)

## Goal
Replace static mock workflows with live functional streams using Supabase. Focus on core Feed and Messaging live data synchronization via Realtime, keeping the UI fully updated without manual refreshes.

## Tasks

<task>
**ID:** 11-02-01
**Title:** Integrate Supabase Feed Realtime Streams
**Description:** Update the `SupabaseFeedRepository` and `FeedCubit` to listen to real-time table streams from `posts` instead of just performing one-off selects.
<read_first>
- lib/features/feed/data/repositories/supabase_feed_repository.dart
- lib/features/feed/presentation/bloc/feed_cubit.dart
</read_first>
<action>
1. In `SupabaseFeedRepository`, add a stream getter: `Stream<List<PostEntity>> watchCircleFeed(String circleId);` via `_client.from('posts').stream(primaryKey: ['id']).eq('circle_id', circleId)`.
2. In `FeedCubit`, subscribe to this stream in `loadFeed` and automatically emit new `FeedLoaded` states when the stream emits data, replacing the manual reload logic. Ensure `StreamSubscription` is canceled in `close()`.
</action>
<acceptance_criteria>
- `supabase_feed_repository.dart` contains `.stream(primaryKey: ['id'])`.
- `FeedCubit` contains a `StreamSubscription` that emits `FeedLoaded`.
</acceptance_criteria>
</task>

<task>
**ID:** 11-02-02
**Title:** Integrate Messaging Realtime
**Description:** Replicate the realtime strategy for messages, handling direct user-to-user DMs and presence via Supabase Realtime subscriptions.
<read_first>
- lib/features/messaging/data/repositories/supabase_messaging_repository.dart
- lib/features/messaging/presentation/blocs/messaging_bloc.dart
</read_first>
<action>
1. Update `SupabaseMessagingRepository` to expose a `watchMessages(String conversationId)` method returning `Stream<List<MessageEntity>>`.
2. Update `MessagingBloc` to use this stream to yield UI state.
</action>
<acceptance_criteria>
- `MessagingBloc` correctly disposes stream subscriptions.
- `supabase_messaging_repository.dart` implements `.stream` for realtime message reception.
</acceptance_criteria>
</task>

## Verification
- Posts created on another client immediately appear in the feed.
- Messages flow sequentially across clients.

## Must Haves
- Safe disposal of `StreamSubscription` blocks.
