---
wave: 3
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/presentation/widgets/tether_walkthrough_overlay.dart
  - lib/features/circles/presentation/screens/circles_screen.dart
autonomous: false
---

# Phase 11, Plan 03: Interactive User Guides (Showcase)

## Goal
Implement interactive overlay user walkthroughs utilizing `showcaseview` package. This aligns with the "safe and calm" project requirement by gently onboarding the user on their first visit sequentially.

## Tasks

<task>
**ID:** 11-03-01
**Title:** Showcase Dependency & Overlay Wrapper
**Description:** Install `showcaseview` and establish an overlay wrapper and helper widget for tracking first visits.
<read_first>
- pubspec.yaml
- lib/core/presentation/widgets/tether_walkthrough_overlay.dart
</read_first>
<action>
1. Add `showcaseview` to `pubspec.yaml`.
2. Create `lib/core/presentation/widgets/tether_walkthrough_overlay.dart`, wrapping child widgets in `Showcase` with consistent styling (e.g., matching the Tether Theme typography and relaxing colors).
3. Connect `SharedPreferences` to track keys like `'has_seen_circles_guide'` and trigger the showcase manually on the screen's `initState` through `WidgetsBinding.instance.addPostFrameCallback`.
</action>
<acceptance_criteria>
- `pubspec.yaml` contains `showcaseview:`.
- `tether_walkthrough_overlay.dart` contains `Showcase` imports and custom styling.
</acceptance_criteria>
</task>

<task>
**ID:** 11-03-02
**Title:** Implement Guides on Core Screens
**Description:** Wrap core actionable items (like Circle creation floating button, feed post input, and check-in prompt) in Showcases.
<read_first>
- lib/features/circles/presentation/screens/circles_screen.dart
</read_first>
<action>
1. In `circles_screen.dart` (or wherever the circle entry resides), wrap the `Scaffold` body in `ShowCaseWidget`.
2. Apply the `TetherWalkthroughOverlay` to the Circle creation button and a core item, providing contextual descriptions like "Create your first safe space here".
3. Trigger the sequence in the initialization block (e.g. `ShowCaseWidget.of(context).startShowCase([...])`).
</action>
<acceptance_criteria>
- `circles_screen.dart` contains `ShowCaseWidget` and `startShowCase`.
</acceptance_criteria>
</task>

## Verification
- User is gently guided when they first enter the Circles screen.
- Overlay descriptions match the "safe and calm" vision.

## Must Haves
- User's state must be tracked (using SharedPreferences) so they don't see it repeatedly.
