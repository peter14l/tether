---
phase: 04-wellness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/mood/domain/entities/mood_status.dart, lib/features/mood/domain/repositories/mood_repository.dart, lib/features/mood/domain/usecases/set_mood_status.dart, lib/features/mood/domain/usecases/get_mood_status.dart, lib/features/mood/data/models/mood_model.dart, lib/features/mood/data/repositories/mood_repository_impl.dart, lib/features/mood/presentation/bloc/mood_cubit.dart, lib/features/mood/presentation/screens/mood_selection_screen.dart]
autonomous: true
requirements: [REQ-06]

must_haves:
  truths:
    - "User can set their mood status and label"
    - "Current mood is persisted in Supabase and visible to Circle members via mood_rooms table"
    - "Mood updates are real-time via Supabase subscriptions"
  artifacts:
    - path: "lib/features/mood/domain/repositories/mood_repository.dart"
      provides: "Mood management contract"
    - path: "lib/features/mood/data/repositories/mood_repository_impl.dart"
      provides: "Supabase implementation of mood management"
    - path: "lib/features/mood/presentation/bloc/mood_cubit.dart"
      provides: "Mood state management"
  key_links:
    - from: "lib/features/mood/data/repositories/mood_repository_impl.dart"
      to: "Supabase 'mood_rooms' table"
      via: "SupabaseClient"
---

<objective>
Implement the Mood Rooms feature including domain, data, and presentation layers.

Purpose: Allow users to express their emotional state to their circles.
Output: Working mood status system with real-time updates and selection UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@TETHER_PRD.md
@lib/features/mood/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mood Domain & Data Layers</name>
  <files>lib/features/mood/domain/entities/mood_status.dart, lib/features/mood/domain/repositories/mood_repository.dart, lib/features/mood/domain/usecases/set_mood_status.dart, lib/features/mood/domain/usecases/get_mood_status.dart, lib/features/mood/data/models/mood_model.dart, lib/features/mood/data/repositories/mood_repository_impl.dart</files>
  <action>Define the Mood feature architecture:
  - Create `MoodStatus` entity with id, userId, status (enum), label, and colorKey.
  - Implement `MoodModel` extending `MoodStatus` with JSON serialization.
  - Create `MoodRepository` interface and its Supabase implementation.
  - Implement `SetMoodStatus` and `GetMoodStatus` use cases.
  - Use `injectable` annotations for repository and use cases.
  
  Repository must handle the `mood_rooms` table in Supabase.
  Status options: need_quiet, anxious, want_to_chat, happy, tired, in_my_head, open_door.</action>
  <verify>
    <automated>dart analyze lib/features/mood/domain lib/features/mood/data</automated>
  </verify>
  <done>Mood domain and data layers are implemented and pass analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Mood Presentation & UI</name>
  <files>lib/features/mood/presentation/bloc/mood_cubit.dart, lib/features/mood/presentation/screens/mood_selection_screen.dart</files>
  <action>Implement the Mood presentation layer:
  - Create `MoodCubit` to manage the user's current mood.
  - Build `MoodSelectionScreen` (or a bottom sheet) allowing the user to select one of the predefined statuses and enter an optional custom label.
  - Add a visual preview of the selected mood (color key mapping).
  - Run `dart run build_runner build` to update dependencies.
  
  UI should follow PRD 4.1 (Time-Adaptive design system) for the selection UI.</action>
  <verify>
    <automated>dart analyze lib/features/mood/presentation</automated>
  </verify>
  <done>User can select their mood through a functional UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Mood status transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | mood_rooms table | mitigate | Supabase RLS policies: user can only modify their own mood row. |
</threat_model>

<verification>
Ensure a user can update their mood and it's saved in Supabase correctly.
</verification>

<success_criteria>
- Mood selection UI is functional and intuitive.
- Mood updates are successfully persisted in Supabase.
- Domain and Data layers strictly follow Clean Architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness/04-01-SUMMARY.md`
</output>
