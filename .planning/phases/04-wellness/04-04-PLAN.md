---
phase: 04-wellness
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified: [lib/core/theme/app_theme.dart, lib/core/theme/time_theme_cubit.dart, lib/features/circles/presentation/widgets/mood_avatar.dart]
autonomous: true
requirements: [REQ-06]

must_haves:
  truths:
    - "App accent color shifts subtly based on the user's current mood status"
    - "Avatars in Circle member lists display a tinted ring indicating their mood status"
  artifacts:
    - path: "lib/features/circles/presentation/widgets/mood_avatar.dart"
      provides: "Avatar with mood ring"
    - path: "lib/core/theme/app_theme.dart"
      provides: "Mood-adaptive theme colors"
  key_links:
    - from: "lib/core/theme/app_theme.dart"
      to: "lib/features/mood/domain/entities/mood_status.dart"
      via: "MoodStatus enum"
---

<objective>
Integrate Mood-Adaptive UI components and shift theme colors based on the current user mood.

Purpose: Provide a more personal and emotionally reactive UI experience.
Output: Dynamic theme shifts and avatar mood rings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/core/theme/
@lib/features/mood/domain/entities/mood_status.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mood-Adaptive Theme Colors</name>
  <files>lib/core/theme/app_theme.dart, lib/core/theme/time_theme_cubit.dart</files>
  <action>Integrate mood-adaptive coloring:
  - Update `AppTheme.getTheme` to accept an optional `MoodStatus`.
  - Implement the logic to shift `accentPrimary` based on the user's mood (PRD 7.5, Feature 70):
    - Calm/Quiet moods (need_quiet, tired) -> shift accent cooler (sage green/blue).
    - Happy moods (happy, open_door) -> shift accent warmer (amber/peach).
    - Anxious moods (anxious, in_my_head) -> slightly desaturate the accent.
  - Update `TimeThemeCubit` to include the current user mood in its state (by listening to `MoodCubit` or similar mechanism).</action>
  <verify>
    <automated>dart analyze lib/core/theme/app_theme.dart</automated>
  </verify>
  <done>UI accent colors dynamically shift based on user mood.</done>
</task>

<task type="auto">
  <name>Task 2: Avatar Mood Ring</name>
  <files>lib/features/circles/presentation/widgets/mood_avatar.dart</files>
  <action>Implement the `MoodAvatar` widget:
  - Create a reusable widget that wraps a circle avatar with a tinted ring.
  - The ring's color and thickness should represent the user's current mood.
  - Use this widget in circle member lists and DM headers where applicable.
  - Ensure the colors match the `colorKey` defined in the mood feature.</action>
  <verify>
    <automated>dart analyze lib/features/circles/presentation/widgets/mood_avatar.dart</automated>
  </verify>
  <done>Mood status is visually represented around user avatars via a soft tinted ring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme → State | Consumption of user state for UI coloring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Theme rendering | accept | UI-only change with no security impact. |
</threat_model>

<verification>
Change mood status in the app and verify that the primary accent color shifts and the avatar ring changes accordingly.
</verification>

<success_criteria>
- Theme colors react to mood changes.
- Mood rings are visible and aesthetically pleasing.
- Integration is clean and doesn't break existing theme logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness/04-04-SUMMARY.md`
</output>
