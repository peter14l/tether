---
phase: 04-wellness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/journal/domain/entities/journal_entry.dart, lib/features/journal/domain/repositories/journal_repository.dart, lib/features/journal/domain/usecases/add_journal_entry.dart, lib/features/journal/domain/usecases/get_journal_entries.dart, lib/features/journal/data/models/journal_model.dart, lib/features/journal/data/repositories/journal_repository_impl.dart, lib/features/journal/presentation/bloc/journal_cubit.dart, lib/features/journal/presentation/screens/journal_screen.dart]
autonomous: true
requirements: [REQ-23]

must_haves:
  truths:
    - "Gratitude journal entries are E2E encrypted client-side"
    - "Supabase only stores the ciphertext of journal entries"
    - "User can write, save, and view their journal history"
  artifacts:
    - path: "lib/features/journal/data/repositories/journal_repository_impl.dart"
      provides: "Encrypted storage for journal entries"
    - path: "lib/features/journal/domain/entities/journal_entry.dart"
      provides: "Journal entry structure"
    - path: "lib/features/journal/presentation/bloc/journal_cubit.dart"
      provides: "Journal state management"
  key_links:
    - from: "lib/features/journal/data/repositories/journal_repository_impl.dart"
      to: "lib/core/utils/encryption_service.dart"
      via: "DI"
    - from: "lib/features/journal/data/repositories/journal_repository_impl.dart"
      to: "Supabase 'gratitude_journal' table"
      via: "SupabaseClient"
---

<objective>
Implement the Encrypted Gratitude Journal feature.

Purpose: Provide a private, safe space for recording gratitude.
Output: Encrypted journaling system with a history view and composition screen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@TETHER_PRD.md
@lib/features/journal/
@lib/core/utils/encryption_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encrypted Journal Domain & Data Layers</name>
  <files>lib/features/journal/domain/entities/journal_entry.dart, lib/features/journal/domain/repositories/journal_repository.dart, lib/features/journal/domain/usecases/add_journal_entry.dart, lib/features/journal/domain/usecases/get_journal_entries.dart, lib/features/journal/data/models/journal_model.dart, lib/features/journal/data/repositories/journal_repository_impl.dart</files>
  <action>Implement the Journal Clean Architecture structure:
  - Create `JournalEntry` entity (id, userId, content, date, circleId).
  - Implement `JournalModel` with JSON serialization.
  - Create `JournalRepository` interface and its Supabase implementation.
  - Use `EncryptionService` to encrypt the `content` field before saving and decrypt it after fetching.
  - Supabase `encrypted_blob` stores the JSON output from `EncryptionService.encrypt`.
  - Implement `AddJournalEntry` and `GetJournalEntries` use cases.
  
  Encryption key should be retrieved or derived (use a dummy key if master key system isn't fully integrated yet, but encryption service calls must be present).</action>
  <verify>
    <automated>dart analyze lib/features/journal/domain lib/features/journal/data</automated>
  </verify>
  <done>Journal entries are successfully encrypted before storage and decrypted on retrieval.</done>
</task>

<task type="auto">
  <name>Task 2: Journal Presentation & UI</name>
  <files>lib/features/journal/presentation/bloc/journal_cubit.dart, lib/features/journal/presentation/screens/journal_screen.dart</files>
  <action>Build the journaling UI:
  - Create `JournalCubit` to handle entry submission and loading.
  - Build `JournalScreen` with two views:
    - Create: A simple compose screen for writing gratitude.
    - History: A scrollable list of past journal entries (cards).
  - UI should use the current time-theme colors.
  - Ensure `dart run build_runner build` is executed for DI generation.</action>
  <verify>
    <automated>dart analyze lib/features/journal/presentation</automated>
  </verify>
  <done>Journaling screens are functional and aesthetically consistent with the PRD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase | Transmission of encrypted blobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | gratitude_journal | mitigate | Client-side encryption ensures Supabase never sees plaintext journal content. |
</threat_model>

<verification>
Save a journal entry, then verify the database contains ciphertext, then verify the app displays the original plaintext.
</verification>

<success_criteria>
- Journal entries are correctly saved in and retrieved from Supabase.
- Content is encrypted in Supabase and plaintext in the app.
- Screens are functional and follow the design principles.
</success_criteria>

<output>
After completion, create `.planning/phases/04-wellness/04-02-SUMMARY.md`
</output>
